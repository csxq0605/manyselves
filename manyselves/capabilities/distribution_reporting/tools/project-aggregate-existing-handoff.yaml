id: project-aggregate-existing-handoff
version: 1.0.0
description: Keep the prepared aggregate context beside the typed editor result
implementation: capability:distribution-reporting:project-aggregate-existing-handoff
input_contract: aggregate_existing_handoff
output_contract: aggregate_existing_handoff
side_effect: pure_read
parallel_safe: true
reuse_result: false
model_visible: false
