id: prepare-template-distillation
version: 1.0.0
description: Freeze the template source and persist the typed distillation input
implementation: capability:distribution-reporting:prepare-template-distillation
input_contract: template_distillation_input
output_contract: template_distillation_input
side_effect: run_local_write
parallel_safe: false
reuse_result: false
model_visible: false
