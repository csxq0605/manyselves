# Final Architecture Consolidation Status

> Manyselves 最终定义驱动架构的唯一跨会话实施状态
>
> Runtime State、Provider Trace、Conversation、Artifact 和 Event Log 不属于本文件
>
> 状态：**自动架构与产品化实施已完成；最终真实 Provider 验收按用户要求暂缓**

## Program

- Repository: `csxq0605/manyselves`
- Plan base: `agent/declarative-runtime-plan`
- Implementation branch: `agent/declarative-runtime-implementation`
- Draft PR: <https://github.com/csxq0605/manyselves/pull/3>
- Governing architecture: [`../architecture/AI_NATIVE_RUNTIME_EXTRACTION_PLAN.md`](../architecture/AI_NATIVE_RUNTIME_EXTRACTION_PLAN.md)
- Execution protocol: [`../CODEX_AUTONOMOUS_EXECUTION.md`](../CODEX_AUTONOMOUS_EXECUTION.md)

## Current position

- Current FA work package: `FA-08 — Post-audit architecture and product convergence (automatic scope complete)`
- Current slice: `FA-08/M9.44 final automatic architecture and release audit`
- Current branch at slice start: `agent/declarative-runtime-implementation`
- HEAD at slice start: `0bf26b0 Runtime: remove unused pre-send gate logic`
- Program status: `automatic scope complete; final real acceptance deferred`
- Final real-test status: `deferred by user; automatic architecture and product prerequisites are complete`
- Blockers: `none；仍禁止未经说明新增身份、Hash/CAS、锁、Gate 或校验算法`
- Next automatic action: `none；等待用户恢复最终真实 Provider/项目/浏览器验收`

## Why the prior completion claim is reopened

原 `WP-00`～`WP-12` 和后续审计建立了真实可用的 Definition Loader/Registry、Workflow Compiler、ResolvedPlan、Stateless Kernel、WorkflowRuntimeHost、Capability definitions、Generic Run API/React，以及声明式 Reporting 的大量文件控制流。

但是，历史“完成”结论采用了现在已经废止的兼容目标：

- Legacy Reporting Runner 保持默认；
- 声明式 Reporting Runner 可以继承旧 `ReportWorkflowRunner`；
- 通用 Workflow Projection 可以把 ReportingFacade 作为 Capability Host；
- `manyselves/core/reporting` 可以继续混合通用 Agent 机制、Reporting 领域逻辑和旧流程；
- 旧 API/旧 Run/双路径行为等价被视为最终完成条件。

用户已经明确：不需要继续以旧流程兼容为目标。最终项目必须形成一个 Stateless Kernel、一个 Generic Compiler/Runtime、Capability-owned Domain Runtime/Tools、文件定义系统和 Generic FastAPI/React。因此旧的兼容式真实 Run 成功不能证明新的最终架构完成。

## Confirmed implemented foundation

以下基础已有生产代码和 focused/affected 测试证据，但仍需在后续拆分中保持：

| Foundation | Current evidence | Completion meaning |
| --- | --- | --- |
| Definition files | Capability/Agent/Task/Tool/Contract/Workflow/Recovery 可从包加载 | 基础已实现，不证明领域实现已正确归属 |
| Registry/Catalog | 两个 Capability 可发现 | 基础已实现，不证明 Application 已与 ReportingFacade 解耦 |
| Compiler | 父子 Workflow、定义快照和完整 Plan 可冻结/恢复 | 核心基础已实现 |
| Stateless Kernel | State/Event 转换和 Host 调度已存在 | Kernel 中立性仍需持续边界审计 |
| Generic Runtime Host | Tool/Agent/Conversation/Subworkflow/Parallel/WAITING 可执行 | Agent/Recovery 生产实现仍部分借用 Reporting 代码 |
| Reporting definitions | Module/Cross/Chief/Final/Delivery 大量流程已文件化 | 不等于声明式 Runner 已脱离 Legacy 父类 |
| Neutral Capability | `parameter-adjustment` 通过同一 Compiler/Host | 证明 Kernel 可中立使用 |
| Generic API/UI | Schema 表单、WAITING、Events、Outputs、Cost 已有通用投影 | 仍需移除 Application 的 Reporting host 耦合并重新审计 |

## Current design-to-production differences

此表是当前实施的权威差异列表。每个切片完成后必须用实时源码更新。

| Final design requirement | Current production evidence at FA-00 start | Difference | Planned correction |
| --- | --- | --- | --- |
| Declarative Reporting 不依赖 Legacy Runner | 五个公开入口由 Capability Binding 直接构造 Compiler/Generic Host/Capability Runtime；生产扫描无旧 Runner/Facade/selector | 已达到；最终真实 Provider 行为仍待一次验证 | FA-07 真实测试 |
| Generic Application 不认识 Reporting service | `WorkflowProjectionFacade(runtime_services=...)` 只经 RuntimeBindingCatalog 选择 Capability | 已达到 | 保持架构 Characterization |
| Reporting Python 归 Capability 所有 | Domain models、lifecycle、Provider composition、Tools、render/delivery 均在 `capabilities/distribution_reporting`；`manyselves/core/reporting` 已删除 | 已达到 | 保持 import/ownership 扫描 |
| Generic Agent/Recovery 不依赖 Reporting | Runtime 拥有 session/turn/recovery vocabulary；Capability 提供 Prompt/typed decoder/progress，并在 Capability Provider composition 机械复用既有 task-attempt persistence | 已达到；最终真实 Provider 行为仍待一次验证 | 保持 Runtime/Kernel 中立边界与 focused Characterization |
| 文件 Workflow 是唯一流程所有者 | 五个公开入口均编译文件定义并由 Generic Host 执行，Module/Cross/Chief/Final/Delivery 作为 Capability Tool/Subworkflow | 已达到 | 真实测试确认长流程 |
| 单一生产入口 | Generic `/runs` + RuntimeBindingCatalog，无 Legacy/declarative selector | 已达到 | 保持 API Characterization |
| 旧兼容代码不在发布图 | 旧 Reporting Runner/Facade/API/Core 包已删除；Legacy Runtime/Tool adapter 名称和实现已删除 | 已达到 | 最终扫描 |
| 文档直面最终形态 | 权威规范、状态、Feature matrix 和网页版真实测试交接均指向 Generic Run Workspace | 已达到；最终结果待真实测试回填 | FA-07 回填 |

## Gate provenance

- Git 全历史中的 `manyselves/capabilities/**/gates/` 从未包含生产 Gate 定义，只有用于建立目录的 `.gitkeep`；因此当前 Gate Definition 数量为 `0` 不是迁移时删除了既有 Gate 文件。
- 旧 Reporting 代码中使用 “gate” 描述的业务语义已按职责迁移：结构校验由 Contract/Pydantic model 表达；Lane all-ready 由 Parallel/Join 表达；review 路由由文件 Workflow 的 `if` 与 Capability Tool 表达；人工异常由 Interaction/`request_input` 表达；恢复动作由 Recovery policy/driver 表达。
- 早期方案预留但从未被生产 Workflow 使用的 `GateDefinition`/`EvaluateGate` 已从 Definition model、Loader、Registry、Compiler、ResolvedPlan 和 Executor Registry 物理删除；没有新增替代门禁，上述业务验收语义继续由现有文件定义机制承担。

## FA work package progress

| Work package | Status | Required evidence |
| --- | --- | --- |
| FA-00 Facts and final specification | `completed` | 六份规范一致；三项生产耦合审计；完成矩阵；diff-check |
| FA-01 Architecture boundary characterization | `completed` | 5 项生产边界 Characterization 已取得真实 RED；Contract 物理归属现已正式转绿，其余 4 项继续以 strict xfail 逐项收敛；Capability 顶层导入纯度已转绿 |
| FA-02 Generic Agent/Conversation/Recovery Runtime | `completed` | Session/turn、定义 Tool/Agent、correction/continuation/no-progress、pre-session result reuse 均由中立 Runtime 生产主调；focused/affected 与生产 spy 证据 |
| FA-03 Distribution Reporting Domain Runtime | `completed` | 五个公开文件入口、Module/Cross/Chief/Final/FinalChief/Aggregate/Template Provider、typed lifecycle、Tools、recovery 和 delivery 均归 Capability |
| FA-04 Generic Capability Runtime Binding | `completed` | `parameter-adjustment` 与 Distribution Reporting 共用通用接口；account Binding 直接构造 Capability Generic Host/Provider composition |
| FA-05 Ownership convergence and legacy deletion | `completed` | 生产/发布图无旧 Runner/Facade/selector/adapter；`manyselves/core/reporting` 与旧专属测试已物理删除 |
| FA-06 Generic FastAPI/React surface | `completed` | Schema/WAITING/Output/Event/Cost 由通用 Run Workspace 投影，无 Capability-ID 流程分支或旧 `/api/v1/reporting` 路由 |
| FA-07 Completion audit and final real test | `automatic audit completed; real test deferred` | 所有生产 Provider 的 completed-result correlation 与自动架构证据已收敛；真实 Provider、真实项目、浏览器测试按用户要求暂缓 |

## Historical evidence retained

历史实现和真实测试仍有价值，但只作为迁移基线：

- 原 `WP-00`～`WP-12` 已实现 definitions/compiler/kernel/runtime/capability/API/UI 的大量基础；
- 2026-08-20 的 Microsoft Agent Framework/LangGraph 隔离 POC 决定为 `reference`；
- focused selections 曾覆盖 Runtime/Compiler/Reporting/API/Frontend；根据用户约束没有执行全量回归；
- `report-declarative-33ed40175b` 曾在真实 Provider、真实项目和浏览器中完成并交付，证明旧兼容式声明路径的业务能力；
- 该历史 Run 不证明移除 Legacy 宿主、ReportingFacade 和 `core/reporting` 混合边界后的最终路径。

详细历史提交、测试数字和旧发现保留在 Git history 与 Draft PR；不再把数百行历史流水复制进当前状态文件。

## Current slice evidence

- FA-00 三项只读审计确认生产链仍为 `Generic /runs → Distribution binding → ReportingFacade → ReportingRunController → ReportingService → DeclarativeReportWorkflowRunner(ReportWorkflowRunner)`；82 个 Capability Contract 中有 41 个 model 路径仍指向 `manyselves.core.reporting.*`。
- FA-00 将 `AGENTS.md`、产品定位、主架构方案、自主执行协议、官方研究和本状态文件统一改写为最终定义驱动形态；`git diff --check` 通过。
- FA-01 新增 `tests/architecture/test_final_runtime_boundaries.py`。实现前 5 项均真实失败，分别覆盖 generic layer import、Binding host、Contract ownership、Runner inheritance 和五个文件入口；当前以 strict xfail 记录尚未修复的生产差异，任何完整修复都会先产生 XPASS，必须同步移除对应标记。
- FA-01 顶层 Capability 导入纯度 Characterization 先失败，随后移除 `distribution_reporting.__init__` 对 compatibility adapters 的 eager import；focused Capability tests `25 passed`，架构边界选择 `5 xfailed`，定向 Ruff 与 `git diff --check` 通过。
- FA-02 `AgentRecoveryDriver` Characterization 先因模块不存在而失败；实现后 `ReportingAgentRunner` 生产路径通过中立 Driver 执行 Recovery event、progress observation、Provider decision 和 attempt snapshot/restore。Runtime/Kernel/Agent Adapter focused 选择 `23 passed`，Reporting recovery 受影响选择 `11 passed, 91 deselected`。
- Generic `InvokeAgentExecutor` 现在从 `AgentInvocationOutcome.session_id` 回写并持久化 `ConversationRecord.external_session_id`，不再要求 Capability Invoker 私下修改 Conversation；新的 outcome-only Invoker Characterization 先失败后转绿。
- 上述 FA-02 切片的定向 Ruff、compileall、5 项架构 strict xfail 和 `git diff --check` 通过；新增代码扫描未发现 Gate、Hash、CAS 或锁逻辑。
- FA-03/FA-05 的首个物理所有权切片将 5 个 Module Lane Contract 类型真正迁到 `capabilities/distribution_reporting/runtime/models/module_lane.py`，删除旧 `core.reporting` 模块且不留 re-export；Capability definition/cohort/direct restore 选择 `30 passed`，旧 Python 路径零引用。41 个旧 Contract model 路径因此减少为 36 个。
- Module Lane 所有权切片的定向 Ruff、compileall、5 项架构 strict xfail 和 `git diff --check` 通过；新增代码扫描未发现 Gate、Hash、CAS 或锁逻辑。
- 紧随的 Module Cohort 所有权切片将 `DeclarativeModuleLaneOutcome` 真正迁入 `capabilities/distribution_reporting/runtime/models/module_cohort.py`，旧 Core 模块不再定义、别名或重导出该类；Contract 动态解析、Join/恢复字段 round-trip 和 extra-forbid Characterization 已覆盖。Definition package `27 passed`、Cohort `3 passed`、Runner affected `6 passed`；Ruff、compileall、旧路径/类扫描和 `git diff --check` 通过。旧 Contract model 路径进一步减少为 35 个。
- Cross Owner 所有权切片将 `DeclarativeCrossOwnerPipelineOutcome` 和 `DeclarativeMainExceptionUserInput` 真正迁入 `capabilities/distribution_reporting/runtime/models/cross_owner.py`，旧 Core 不保留定义、alias 或 re-export；Contract 路径、`__module__`、round-trip 和 extra-forbid 的 3 项 Characterization 实现前 RED、实现后转绿。旧 Contract model 路径从 35 个减少为 33 个。
- 该 Cross affected 选择暴露了一个真实恢复偏差：全局 `module_review_completion_refs` 同时被当作 initial baseline 和 Cross lifecycle completion，导致重试读取错误 subject。修复后 `_restore_completed_module_review` 只按当前 `review_root + revision` 读取 canonical `completion-rN.json`，pipeline 未 Reduce 前不改写全局 baseline，最终 Reduce 再发布 r1；严格 subject 绑定未放宽，也未增加 fallback、Gate、Hash、CAS 或锁。Definition package `30 passed`，Cross concurrency 受影响文件 `22 passed`，精确恢复 `2 passed`，Cohort + Runner resume `2 passed`；Ruff、compileall、路径扫描和 `git diff --check` 通过。
- Chief Chapter 所有权切片将 `DeclarativeChiefChapterAgentResult`、`DeclarativeChiefChapterContext`、`DeclarativeChiefChapterOutcome` 真正迁入 `capabilities/distribution_reporting/runtime/models/chief_chapter.py`，旧 Core 不保留定义、alias 或 re-export。所有权/旧 Core 暴露的 3 项 Characterization 实现前 RED、实现后转绿；Definition package、Chief cohort 与 Runner affected 合计 `36 passed`，Ruff、compileall、旧路径扫描和 `git diff --check` 通过。直接指向 Core Reporting 的 Contract model 路径从 33 个减少为 30 个；新模型仍依赖待后续迁移的 `ChiefChapterLaneInput`、`ChiefChapterLaneSubmission` 和 `TaskEnvelope`，因此不把本切片误报为 Capability 边界完成。
- Final Chapter 所有权切片将 `DeclarativeFinalChapterAgentResult`、`DeclarativeFinalChapterContext`、`DeclarativeFinalChapterOutcome` 真正迁入 `capabilities/distribution_reporting/runtime/models/final_chapter.py`，旧 Core 不保留定义、alias 或 re-export；Final review cycle 也直接消费 Capability Outcome。3 项 Characterization 实现前 RED、实现后转绿；Definition package、Final cohort/nested recovery 与 Runner affected 合计 `45 passed`，主工作区复核选择 `5 passed`，Ruff、compileall、旧路径扫描、5 项架构 strict xfail 和 `git diff --check` 通过。直接 Core Contract 路径从 30 个降至 27 个；`FinalChapterLaneFindingSubmission`、`TaskEnvelope` 和 `FinalChapterLaneInput` 的传递依赖仍待后续领域基础模型迁移。
- Final Review 所有权切片按最小无环连通组件迁移 7 个 YAML 直引模型和其嵌套的 `DeclarativeFinalVerdictRecord`，共 8 个类型进入 `capabilities/distribution_reporting/runtime/models/final_review.py`；只迁 7 个会形成 Capability→Core→Capability 循环，因此没有采用。旧 Core 不保留定义、alias 或 re-export，流程函数仍原地且语义未变。3 项 Characterization 实现前 RED、实现后转绿；Definition package、Final cycle/nested recovery 与 Runner affected `48 passed`，主工作区复核选择 `5 passed`，Ruff、compileall、旧路径扫描、5 项架构 strict xfail 和 `git diff --check` 通过。直接 Core Contract 路径从 27 个降至 20 个；8 个模型仍引用待迁移的 agentic/input 基础类型，因此不声称完整边界完成。
- FA-03/M0 将完整的 run-scoped Reporting taxonomy 从 `core/reporting/taxonomy.py` 真实迁到 `capabilities/distribution_reporting/domain/taxonomy.py`，删除旧文件且不留 alias、re-export 或 shim；48 个生产/测试消费者统一改用 Capability 路径。独立子进程 import Characterization 实现前因新模块缺失取得 `ModuleNotFoundError` RED，迁移后证明不会加载 `manyselves.core.reporting`；旧路径引用为 0。Agent focused `37 passed`，主工作区 taxonomy/models/ownership 选择 `18 passed`，新模块/ownership Ruff、全部消费者 import-order Ruff、compileall、5 项架构 strict xfail 和 `git diff --check` 通过。`hashlib` 仅是旧 workbook snapshot 行为原样搬迁，没有新增或扩展 Hash/CAS 逻辑。
- FA-03/M1 将完整 Reporting 基础模型组件从 `core/reporting/models.py` 真实迁到 `capabilities/distribution_reporting/runtime/models/reporting.py`，包括 19 个模型/枚举、15 个常量/类型别名、`chapter_section_ids` 与 `ReportRequest`；旧文件、Core re-export、无消费者的 Cross lazy alias 均删除，不保留 shim。`distribution_reporting_input.yaml` 及 77 个生产/测试直接消费者改用 Capability 路径；Capability 树内直接 Core Reporting 路径由当前 HEAD 的 23 个降到 20 个。两项物理归属/独立导入 Characterization 实现前 `2 failed`、实现后转绿；Agent affected 选择累计 `216 passed`，主工作区模型/taxonomy/定义投影/Binding 复核 `20 passed`，定向 Ruff、全部改动 import-order Ruff、compileall、旧路径扫描、5 项架构 strict xfail 和 `git diff --check` 通过。原 validator/schema/default/SHA 字段保持不变，未新增或扩大 Hash、CAS、锁、Gate 或验证逻辑。
- FA-03/M2 将 52 个类、3 个函数和 19 项 `SUBMISSION_INPUT_TYPES` 组成的完整 agentic schema registry 从 `core/reporting/agentic_models.py` 真实迁到 `capabilities/distribution_reporting/runtime/models/agentic.py`；旧文件删除且不留 alias、re-export 或 shim。42 个生产消费者、35 个测试消费者和 10 个 YAML 动态模型路径已更新；首次迁移后的 ownership RED 暴露审计漏记的 3 个 `core/tools` 消费者，扩大扫描补齐后旧路径归零。新组件只依赖 sibling Reporting 模型、Capability taxonomy、yaml/Pydantic，不导入 Core Reporting；Capability 树内直接 Core Reporting 路径由 20 个降到 14 个，其中 YAML Contract 只剩 10 个。Characterization 从独立导入 `1 failed`、首次 ownership `1 failed` 转为 `3 passed`；Agent affected 五组累计 `216 passed`，主工作区 ownership/agentic/definition 复核 `62 passed`，定向 Ruff、全部改动 import-order Ruff、compileall、AST 逻辑等价、旧路径扫描、5 项架构 strict xfail 和 `git diff --check` 通过。所有 schema/validator/default 逻辑保持不变，未新增或扩大 Hash、CAS、锁、Gate 或验证逻辑。
- FA-03 实时生产链复审确认：Generic `/runs` 虽先选择 definition-owned Binding，但 Distribution binding 仍调用 `ReportingFacade.start_declarative → ReportingRunController → ReportingService._execute_locked → DeclarativeReportWorkflowRunner(ReportWorkflowRunner).run`；父 `run()` 做完 prepare/dispatch 后才通过子类 `_run_module_lanes()` 把 `WorkflowRuntimeHost` 嵌入中段，Host 返回后再回到旧 `run()`。因此当前不是“一个顶层 Host”，文件 Workflow 也不是唯一控制流。render/distill/aggregate 仍完全绕过 Host，Capability 也只声明一个 `distribution-reporting` entrypoint。FA-03 顺序据此修正为 M3 Contracts/Inputs、M4 Capability State/Recovery、M5 Review models/Cross contracts、M6 Preparation/Deterministic domain、M7 Rendering/Materialized Delivery、M8 Review Action Runtime 与五个文件 entrypoint；不得把 13 模块 Legacy SCC 整体换目录冒充解耦。
- FA-03/M3 将 `submission_contracts.py` 和 `input_contracts.py` 作为单向无环组件分别迁到 Capability `runtime/contracts/submissions.py` 与 `runtime/models/inputs.py`，旧文件删除且不留 alias、re-export 或 shim；Submissions 的 4 个生产/2 个测试消费者、Inputs 的 13 个生产/19 个测试消费者以及 5 个 YAML 动态模型路径全部更新。独立导入 Characterization 实现前 `1 failed`，迁移后 ownership/schema/example/field-guidance/动态 Contract `4 passed`；Agent affected 五组累计 `197 passed`，主工作区 ownership/definition/agentic/chapter contract 复核 `71 passed`，定向 Ruff、全部改动 import-order Ruff、compileall、归一化 AST 语义等价、旧路径扫描、5 项架构 strict xfail 和 `git diff --check` 通过。仅删除两个被 Python 后值覆盖的重复死键和两个未使用 import，未改变 validator/default/判断语义，也未新增 Hash、CAS、锁、Gate 或依赖。Capability 对 Core Reporting 的剩余直接耦合已缩为 `module_lane.py` 的 state/review types、4 个 Cross YAML 和待删 compatibility adapters。
- FA-03/M4 将 Reporting Capability 自有的 `parallel_runtime.py`、`store.py`、`source_ledger.py` 分别真实迁到 `runtime/state/parallel.py`、`runtime/storage.py`、`runtime/source_ledger.py`，删除无消费者 `recovery_state.py` 及仅供该 shim 的 4 个兼容 alias，不留 re-export/shim；20/11/7 个生产消费者与 8/13/5 个测试消费者已改用新路径。新路径缺失 Characterization 先 `1 failed`，迁移后独立导入/物理归属/旧 spec `2 passed`；Agent focused 累计 `97 passed`，主工作区 State/Recovery/Ready/Store/Ledger 复核 `34 passed`，定向 Ruff、全部改动 import-order Ruff、compileall、逐节点 AST 等价、旧路径扫描、5 项架构 strict xfail 和 `git diff --check` 通过。原有 hash、lock、lease、fcntl、digest、validator 和状态算法仅机械迁移，未新增或扩大；这些仍属于 Capability state/recovery，不进入 Kernel。Capability 对 Core Reporting 的直接耦合现仅剩 `module_lane.py` 的 Review models、4 个 Cross YAML 和待删 compatibility adapters。
- FA-03/M5 从 7,500 行混合 `review_lifecycle.py` 中只迁出 23 个纯 Review 状态模型到 Capability `runtime/models/review.py`，并将 4 个 Cross YAML 直引结果/Context 模型迁入现有 `runtime/models/cross_owner.py`；Coordinator、I/O、验证函数、Invoker、Runtime、compile/retry 和旧循环均未随模型搬迁。旧模块不定义、不 re-export，4 个 YAML 与所有直接消费者已更新；Capability 生产 Python/YAML 对 Core Reporting 的直接引用现仅剩待删 compatibility adapters。Ownership/definition 主复核 `46 passed`，Cross/Module/Chapter affected `7 passed`，Ruff、全部改动 import-order Ruff、compileall、旧路径扫描和 `git diff --check` 通过。架构 Contract ownership 首次产生 strict XPASS，移除临时标记后正式为 `1 passed, 4 xfailed`；未新增或扩大 validator、Gate、Hash、CAS、锁或依赖。
- FA-03/M6.1 将 `claim_ledger.py`、`coverage.py`、`revision_diff.py`、`cross_specialization.py`、`final_specialization.py` 五个纯确定性领域模块真实迁入 Capability `domain/`，旧 Core 文件删除且不留 alias、re-export 或 shim；Workflow、Review、Service、协作 Tool、Renderer、Final cohort/cycle 与测试消费者全部直接使用 Capability 路径。新路径缺失 Characterization 先 RED，迁移后 ownership/独立导入/旧 spec/固定输出 `3 passed`；Agent focused 累计 `30 passed`，主工作区 ownership + 五个行为文件 `22 passed`，Ruff、全部改动 import-order Ruff、compileall、逐字节等价、旧路径扫描、架构 `1 passed, 4 xfailed` 和 `git diff --check` 通过。没有新增或改变 validator、Gate、Hash、CAS、锁、依赖或业务判断。
- FA-03/M6.2 将 canonical Markdown 规则从 `core/reporting/report_markdown.py` 真实迁到 Capability `domain/report_markdown.py`，旧文件删除且不留 alias、re-export 或 shim；Workflow、PDS Renderer 与测试消费者直接使用 Capability 路径。新路径缺失 Characterization 先 `1 failed`，迁移后 ownership/独立导入/固定 `CanonicalReportContent`、heading、重复表、Chapter 4 顺序输出 `3 passed`；Agent focused `13 passed`，主工作区 ownership/deterministic + Renderer（排除下述既有固定源码 SHA）`34 passed`，Ruff、import-order Ruff、compileall、逐字节等价、旧路径扫描和 `git diff --check` 通过。额外运行整个 Renderer 文件暴露既有 `test_packaged_v2_core_matches_normalized_handoff_source` 失败：当前 HEAD 与工作树 `v2_docx_renderer.py` SHA 均为 `5edc…`，测试仍硬编码旧 `b14c…`；M6.2 未修改该文件。按 M7 已审计方案不更新为新 Hash，而以 renderer 动态入口/行为 Characterization 取代该 Legacy 固定源码 Hash 门禁。
- FA-03/M6.3 建立 Capability `runtime/models/preparation.py`，真实拥有 `ManifestFile`、`ProjectManifest`、`ParsedArtifact(s)`、`FilePreparationResult`、`MappingGap/Result`，旧 Reporting/Preparation/Mapper 模块不再定义或 re-export；现有字段、validator/default 与 `mapping_gaps: list[dict]` 保持不变。`project_manifest`、`parsed_artifacts`、`evidence_items`、`coverage_matrix`、`output_artifacts` 五个原 `{}` 占位 Contract 已改为真实 Pydantic 模型或列表别名，不新增 Contract ID。Luna worker focused 为 ownership `4`、definition `42`、preparation `3`、intake `13`、taxonomy `5`、service boundary `46`、agent workflow `3` passed；主工作区 ownership/definition/preparation `49 passed`。Ruff、import-order Ruff、compileall、旧定义/反向依赖扫描、架构 `1 passed, 4 xfailed` 和 `git diff --check` 通过；没有新增 validator、Gate、Hash、CAS、锁、I/O 或依赖。
- FA-03/M6.4 将 `intake/{adapters,manifest,workbook,wps_images}`、`mappers/{common,s2_1,s4_4,s4_6}` 与 `preparation.py` 真实迁入 Capability `runtime/`，删除旧 Core 文件/目录且不留 shim；Intake package 不再保留无消费者 eager API，Mapper package 只聚合 3 个 mapper。Service、Workflow、测试和 monkeypatch 路径全部更新。Characterization 在新路径缺失时先 `3 failed`，迁移后 Runtime ownership `11`、M6.3 ownership `4`、Intake `19`、Mappers `17`、Preparation `3`、Service normalization `1` passed；主工作区复核 ownership/Intake/Mapper/Preparation `54 passed`。Ruff、import-order Ruff、compileall、旧路径/反向依赖扫描、架构 `1 passed, 4 xfailed` 和 `git diff --check` 通过。Manifest、Evidence ID、WPS Photo 三处既有 SHA-256 与既有文件判断仅机械迁移，未新增或扩大 Hash、CAS、锁、Gate、validator 或依赖。
- FA-03/M6.5a 新增 Capability-owned `PreparationContext`、单一 typed `preparation_context` Contract、7 个非模型可见 Tool Definition 和纯 Tool 的 `distribution-reporting-preparation` 文件工作流；正常分支按 Manifest→Taxonomy→Parse→Normalize→Coverage→Persist 顺序执行，恢复分支只执行 Restore，顶层文件 Workflow 已在 Module Cohort 前声明该子工作流。Characterization First 先因上述边界不存在取得 `5 failed`，实现后 `5 passed`；受影响 Definition/现有声明式阶段 `10 passed`，Ruff、compileall、架构 `1 passed, 4 xfailed` 与 `git diff --check` 通过。当前 Legacy 父 Runner 已完成 Preparation，因此过渡编译状态显式设 `preparation-complete=true` 并仅注册不会被执行的 passthrough，以避免在 M6.5b 真正 Tool 接入前重放；它不是最终路径，下一切片删除。没有新增 Hash、CAS、锁、Gate、validator、Agent/Provider 调用、Action Kind 或依赖。
- FA-03/M6.5b 新增 Capability `runtime/preparation_tools.py` 与 `preparation_snapshot.py`：Manifest、Taxonomy、文件 worker、Evidence/Photo 归一化、Coverage、canonical Persist/Restore 均以 `PreparationContext` 为唯一值，并由 `build_preparation_tool_implementations` 精确绑定 7 个 YAML Tool ID。输入快照、content snapshot 与照片 ID 逻辑通过窄构造依赖注入，Capability 不导入 `core.reporting`；Normalize 删除旧 direct-mapper fallback。Canonical snapshot 使用既有 `ReportingStore`、staging directory 与 `os.replace`，completion 不再复制旧 `preparation_sha256`、CAS、FullReportCheckpoint、workflow-state 比对或 photo repair。Characterization 在模块缺失时分别先 `3 failed` 与 `ModuleNotFoundError`，实现后 Preparation focused `14 passed`；现有声明式父 Runner 的 passthrough 已删除，旧 mid-stage 编译只移除已由父 Runner 完成的两个 Preparation Action，最终 Generic Run service 将原样编译文件 Workflow。没有新增 Hash、CAS、锁、Gate、validator、Agent/Provider 调用、Action Kind 或依赖。
- FA-03/M6.6 将 Reporting 专属 `special_topics.py` 与 `research/project_evidence.py` 真实迁入 Capability runtime，并从旧 `assets.py` 只提取纯 `runtime_photo_ids` 到 Capability domain；旧两个模块删除且不留 shim/re-export，Workflow、Service、Chief cohort、Research Tool 与测试消费者均直接使用新路径。Characterization 在新路径缺失时先出现 3 个 import error，迁移后 ownership `2 passed`；主工作区 special-topic/evidence/photo affected `33 passed`。Ruff、新消费者 import-order Ruff、compileall、逐字节迁移、旧路径扫描和 `git diff --check` 通过。专项来源 SHA 与 Evidence snapshot/content SHA 只是原逻辑机械迁移，未新增或扩大 Hash、CAS、锁、Gate、validator 或依赖。
- FA-03/M6.6a 将专项问题计划加载、Evidence Index 与 Source Ledger 物化显式接入 Capability-owned Preparation 文件工作流：`load-special-topic-plan` 在 Persist 前运行，`finalize-preparation` 在新建与恢复分支的 Persist/Restore 后运行，并把 canonical refs 回写 `PreparationContext`；两者均是 YAML 声明的非模型可见 Tool，Kernel、Compiler、Host 无 Reporting 分支。Characterization First 先 `4 failed`，实现后 Agent focused `48 passed`；主工作区 Preparation/definition/架构复核 `19 passed, 4 xfailed`，定向 Ruff 与 `git diff --check` 通过。实现仅调用 M6.6 已迁入 Capability 的既有 `ProjectEvidenceIndex`/`SourceLedger`，没有新增或复制 Hash、CAS、锁、Gate、validator 或依赖。
- FA-03/M6.7 新增 Capability-owned Evidence Readiness domain/runtime/models、6 个 typed Contract、Interaction、Output、4 个非模型可见 Tool 与独立文件 Workflow。`ask` 在通用 `RequestInput` 前先用 `PublishResult` 暴露缺失证据/受影响模块，`block` 使用既有 `FailWorkflow`，`draft/skip` 直接发布并结束；`supplement` 只经 typed Tool 投影后，以现有 `Subworkflow` 真实重跑 `distribution-reporting-preparation`，再用 `Goto` 在同一 Run 重新评估，不引入 callback/Runner/Provider 或 Kernel 业务 Action。Characterization First 初始 `3 failed`、supplement 子工作流另取得 `1 failed`，实现后新 focused `9 passed`，Definition/Preparation affected `47 passed`；定向 Ruff、compileall 与 `git diff --check` 通过。未新增 Hash、CAS、锁、Gate、validator 或依赖；顶层 operation 对该子工作流的组合留在 M8 文件 entrypoint 切片完成。
- FA-03/M6.7c 将仍被过渡父流程消费的 `EvidenceReadinessDecision`、`EvidenceReadinessPolicy` 与 `ReportingBlockedError` 原样迁入 Capability domain，删除旧 `core/reporting/evidence_readiness.py` 且不留 shim/re-export；Workflow、Service 与既有行为测试均直接使用 Capability 路径。所有权 Characterization 先 `2 failed`，迁移后新/文件化 Readiness `11 passed`，旧政策/恢复/Service affected 独立选择 `50 passed`；定向 Ruff、compileall、旧路径扫描与 `git diff --check` 通过。没有改变 ask/block/draft/skip 判断，也未新增 validator、Gate、Hash、CAS、锁或依赖。
- FA-03/M7.1 将 `core/reporting/rendering` 的 7 个确定性渲染叶子真实迁入 Capability `runtime/rendering`，旧目录删除且不留 shim/re-export；Assets、Service、Workflow 与渲染测试消费者均改用 Capability 路径。Characterization 在新路径缺失时先 `3 failed`，迁移后 ownership/独立导入/最小 DOCX 行为 `3 passed`；旧 `v2_docx_renderer.py` 固定源码 SHA 测试已删除，改为动态加载入口、生成可打开 DOCX 并验证标题/正文保留，不更新 Hash。Agent focused 为 ownership + renderer `33 passed`、直接 affected `82 passed`；主工作区渲染/Service/Web/架构选择 `103 passed, 4 xfailed`，import-order Ruff、compileall 与 `git diff --check` 通过。默认模板路径随物理归属修正到现有 Capability 资源位置；没有新增 Hash、CAS、锁、Gate、validator 或依赖，现有渲染结果 SHA 字段未扩大为决策条件。
- FA-03/M7.2a 新增 Capability `runtime/models/delivery.py`，真实拥有三个文件化 Delivery 阶段共享的 `DeliveryContext` 与当前普通文件交付使用的 `MaterializedDeliveryReceipt`；旧 Workflow/Core Delivery 不再定义或 re-export，Declarative Delivery、Runner 与测试消费者均直接使用 Capability 类型。Characterization 在模块缺失时先 `1 failed`，迁移后 ownership/独立 import/typed JSON round-trip 与直接阶段测试 `12 passed`，Agent Delivery 核心选择 `30 passed`；主工作区 delivery/tail/架构定向选择 `14 passed, 39 deselected`，定向 Ruff、compileall 与 `git diff --check` 通过。`DeliveryPackage`、旧 `DeliveryReceipt`、`ProjectDelivery` 及其 CAS/hash/lock 兼容子图未迁移、复制或修改；`MaterializedDeliveryReceipt` 的既有路径 validator 仅机械迁移，没有新增判断门禁或依赖。
- FA-03/M7.2b 新增 Capability `runtime/delivery_tools.py`，让文件定义 `publish-materialize-delivery` 直接完成公共 Markdown/DOCX、五个 Module Markdown、同 Run 普通文件 Delivery package/manifest/receipt；生产 `DeclarativeDeliveryRuntime.publish` 不再调用 Runner 私有 publish，旧 `ReportWorkflowRunner._publish_and_materialize_delivery` 只保留为 Legacy `_deliver` 的 Capability 薄委托。Characterization First 先以私有 Runner spy 取得 `1 failed`，实现后 Delivery/声明式 Tail focused `10 passed`，受影响顶层 Tail/恢复/Delivery root 选择 `5 passed`；定向 Ruff、import-order Ruff、compileall 与 `git diff --check` 通过。新实现没有导入 Core Reporting，没有引入 ProjectDelivery、CAS、Hash、Trusted Handle、版本发布或锁；普通原子文件复制只保留源文件/工作区路径前置错误，未形成业务状态门禁。
- FA-03/M7.2c 将文件定义 `prepare-render-delivery` 的完整产物编排迁入 Capability `DeliveryTools.prepare`：从 JSON roundtrip 后的 Run state 自行恢复既有 typed models，写出 claims/sources/evidence、五个 approved modules、request/photo snapshots、report-state、最终 Markdown/DOCX、来源索引、模板快照/provenance 以及 render request/result/log，并返回可持久化 `DeliveryContext`。生产 `DeclarativeDeliveryRuntime.prepare` 不再调用 Runner 私有完整 prepare，旧 `_prepare_and_render_delivery` 只剩 Capability 薄委托；当前 final-audit subject、handoff contracts、delivery projection、既有结构验证和模板解析仍以五个窄私有回调注入，后续 Review/Domain Runtime 切片继续物理迁移，未把 Runner/Service 传入 Capability。Characterization First 先因旧 Runtime 无 `prepare_tool` 取得 RED，真实产物测试与 Delivery focused `5 passed`、Tail 恢复选择 `5 passed`、顶层 delivery/tail 选择 `2 passed`；主工作区 Delivery/架构复核 `6 passed, 4 xfailed`，定向 Ruff、import-order Ruff、compileall 与 `git diff --check` 通过。Capability 不导入 Core Reporting，没有新增 Hash/CAS/锁/lease/Provider/依赖或 Renderer `before_publish` 回调；唯一新增错误是缺少 Tool/回调绑定时的配置错误，不参与业务状态判断。
- FA-03/M7.2d 将文件定义 `complete-delivery` 的当前 Run 完成语义迁入 Capability `DeliveryTools.complete`：从已发布且 JSON roundtrip 的 `DeliveryContext` 生成精确 11 个 `OutputArtifact`、`delivery_status`、`delivery_completion_ref` 与 `delivery-completion.json`，保留 final review/final audit 当前 Run 证据并清理阶段 Context。生产 Runtime 不再调用 Runner 私有 complete，旧 `_complete_delivery` 只剩 Capability 薄委托；Core 原有 `ReportVersion`、`SessionSummaryStore`、additional-artifacts/version-warning 逻辑从这条路径删除，新路径不写 `Work/report-versions`。Characterization First 先证明旧路径调用私有 Runner complete 取得 `1 failed`，Capability complete/Delivery focused `6 passed`，声明式 delivery/tail 选择 `7 passed, 34 deselected`；主工作区直接复核 `4 passed`、架构 `1 passed, 4 xfailed`，定向 Ruff、import-order Ruff、compileall 与 `git diff --check` 通过。未引入 ProjectDelivery、Hash/CAS/blob/trusted handle/storage version、锁/lease、Provider、额外业务门禁、依赖或公共接口。
- FA-03/M7.3a 将 436 行确定性资产装配与 Markdown 验证组件从 `core/reporting/assets.py` 真实迁到 Capability `runtime/assets.py`，删除旧文件且不留 shim/re-export；Review、Workflow 与三个测试消费者全部直接使用 Capability 路径。新路径独立子进程 import Characterization 先因模块不存在 RED，迁移后证明不加载 `manyselves.core.reporting`、符号物理归属正确且旧路径引用为 0。Agent ownership/assets `27 passed`、Markdown/Projection `17 passed`、deterministic final gate `4 passed`、Delivery `3 passed`；主工作区 ownership/assets/Delivery prepare 复核 `28 passed`，定向 Ruff、import-order Ruff、compileall 与 `git diff --check` 通过。实现逐行机械迁移，未新增或改变 validator、判断门禁、Hash/CAS、锁/lease、依赖或公共接口。
- FA-03/M7.3b 新增 Capability-owned `runtime/delivery_projection.py` 与 `runtime/report_validation.py`，将引用绑定投影及最终报告结构校验从 Runner 私有回调迁入 Delivery Tool 直接调用；`_DeliveryPreparationDependencies` 由五个窄回调减为 final-audit subject、handoff contracts 与 template resolver 三个，生产 `DeclarativeDeliveryRuntime.prepare` 不再访问 Runner 私有 projection/validation，Core 同名方法只保留 Legacy 薄包装与原 `AgentWorkflowError` 转换。Characterization 覆盖投影等价、成功/失败校验报告持久化和生产 prepare 禁止调用两个 Runner 私有方法；Delivery focused `3 passed`，最终结构/声明式交付/架构选择 `9 passed, 4 xfailed`，定向 Ruff、compileall 与 `git diff --check` 通过。未修改 Kernel/Compiler/Host，未新增或扩大 validator、业务判断门禁、Hash/CAS、锁/lease、Provider、依赖或公共接口；M7.3 下一切片继续迁出 handoff contracts 与 template resolver。
- FA-03/M7.3c 将交付 handoff contract matrix 与 report template selection 分别物理迁入 Capability `runtime/handoff_contracts.py` 和 `runtime/template_resolver.py`；`DeliveryTools.prepare` 直接使用两个 Capability 函数，生产 `DeclarativeDeliveryRuntime` 不再访问 Runner/Service 的两个私有回调，Core `ReportWorkflowRunner._write_handoff_contracts` 和 `ReportingService.resolve_report_template` 只保留 Legacy 薄包装。Characterization First 先因生产调用 Runner 私有 handoff 方法取得 `1 failed`，GREEN 后同时断言七阶段 `handoff-contracts.json`、packaged template provenance 和两个私有 spy 均不被调用。中间 affected 复核真实捕获并修复了常量改名残留导致的两个 `NameError`；修复后 Delivery/Template/Input Snapshot `5 passed`，Capability Delivery 与架构选择 `9 passed, 4 xfailed`，定向 Ruff、import-order Ruff、compileall 与 `git diff --check` 通过。新 Capability 实现不导入 Core，未新增 Hash/CAS、锁/lease、Provider、validator、业务门禁、依赖或公共接口；旧 handoff 中的 `hash-verified` 仅是未改动的历史文案，没有被扩成运行逻辑。M7.3 下一切片迁出最后一个 final-audit subject/review artifact 依赖。
- FA-03/M7.3d 新增 Capability-owned `runtime/review_artifacts.py`，真实拥有 current-run Review Completion 读取、Cross synthesis/Final residual risk 投影和 final-audit subject 绑定；`DeliveryTools.prepare` 直接调用 Capability 实现，`_DeliveryPreparationDependencies` 与全部 Runner 回调注入删除，生产 `DeclarativeDeliveryRuntime` 不再访问 Runner 私有 final-audit 方法，Core 同名方法仅保留 Legacy 薄包装与原错误类型转换。Characterization First 先以 Runner 私有方法 spy 取得 RED，GREEN 后覆盖 `final-chapter-wave` 多轮 finding/verdict、缺失 snapshot 的同 Run 确定性重建、snapshot/validation identity 和审计后内存报告不可漂移；重建只投影已完成的精确 subject，不重放 Provider、Reviewer 或 Conversation。Delivery/Review/架构 affected `17 passed, 4 xfailed`，定向 Ruff、import-order Ruff、compileall 与 `git diff --check` 通过。Capability 未导入 Core，没有新增或扩大 validator、业务门禁、Hash/CAS、锁/lease、Provider、依赖或公共接口；M7.3 至此完成，下一步执行 M8 五个真实文件 Workflow 入口和顶层 Host 编排迁移。
- FA-03/M8.0 新增 Capability-owned `ReportingRunContext`、Initializer/Preparation Attachment typed 输入与三个文件声明的非模型可见投影 Tool；公开 `ReportRequest`、Host Run ID 和 `PreparationContext` 现在通过显式类型连接，并把 Preparation 的 snapshot refs、Evidence/Photo/Coverage/Taxonomy/专项计划及固定模块选择投影到下游阶段状态，不再要求父 Runner 隐式拼装这一边界。Characterization First 在模块不存在时取得 `ModuleNotFoundError` RED，实现后 Context/Definition focused `3 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片只建立 M8 顶层文件 Workflow 的数据连接，不宣称任何公开入口已完成；未新增业务 validator/Gate、Hash/CAS、锁/lease、Provider、Action Kind、依赖或公共接口。
- FA-03/M8.0a 将 render/preparation 共用的既有 `RunInputSnapshotStore` 及其 typed snapshot models 物理迁入 Capability `runtime/input_snapshot.py`，旧 Core Reporting 文件删除且不留 shim/re-export；Service、Workflow、Agent Runner、Knowledge Context 和测试消费者全部直接使用新路径。Capability 模块独立导入 Characterization 先因新路径缺失 RED，迁移后 ownership + 原 snapshot focused `3 passed`；原冻结、inventory digest、Content Store、trusted handle 与锁实现逐行保留，只把基础 Content Store import 延迟到实例构造，避免导入 Capability model 时触发历史 `manyselves.core` 的 eager Reporting 副作用。没有新增、复制或扩大 Hash/CAS、锁、validator、Gate、依赖或公共接口；该切片为 `render-existing` 文件 Tool 消费同一 Run frozen source 提供所有权边界。
- FA-03/M8.1 新增真实公开 `render-existing` 文件入口：`DistributionReportingRuntimeBinding.start` 选择 `render-existing.yaml`，经 `WorkflowCompiler` 编译后由 `WorkflowRuntimeHost` 顺序执行 `prepare-render-existing` 与 `render-existing` 两个 Capability Tool；源文件冻结发生在 prepare Tool 内，后续只消费同一 Run 的 frozen path、模板 provenance 与物化模板，最终 DOCX、验证日志、运行状态、事件和输出均由 Host 投影。Characterization First 先因入口缺失取得 `1 failed`，GREEN 后入口/服务 focused `4 passed`，Web 投影与最终架构边界 `15 passed, 4 xfailed`，定向 Ruff、compileall 与 `git diff --check` 通过。四项 xfail 仍对应尚未迁移的其他入口/Legacy 生产图；当前 adapter 中按 Workflow ID 选择逐入口 Runtime 及各 Runtime 重复的 compile/host 装配是 FA-04 必须统一的临时债务，不视为最终 Binding。未新增 Provider、Agent、Hash/CAS、锁/lease、validator、业务 Gate、依赖或公共接口。
- FA-03/M8.2a 为 `distill-template-skill` 建立可编译、可由 Host 执行的完整 Capability 内部链：文件 Workflow 在 `prepare-template-distillation` Tool 内复用既有 Run snapshot 冻结模板，再以 typed contract 创建固定 `template-distillation` Conversation、调用 `template-distiller` Agent、由文件 Tool 物化 14 个 `SKILL.md`、boundary/source manifest，并发布 typed output；`TemplateDistillationWorkflowRuntime` 只组合 `WorkflowCompiler + WorkflowRuntimeHost` 与注入的 AgentInvoker。Characterization First 先证明旧 Agent contract 使 Compiler 拒绝，GREEN 后 focused `6 passed`、既有 Template Runner `1 passed`，主工作区 distill/aggregate 选择 `12 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；旧 `output_artifacts` accepts/produces 保留并增加 typed contract，避免破坏现有模板语料投影。当前尚未加入公开 Capability entrypoints，也未接真实 Provider Binding，不宣称该公开 operation 完成。该切片只复用既有 snapshot/CAS 行为，没有新增或扩大 Hash/CAS、锁/lease、validator、Gate、依赖或公共接口。
- FA-03/M8.2b 新增最小 Capability-owned `TemplateDistillationAgentBridge`，直接组合中立 `AgentExecutionService.start_or_restore + dispatch_turn`：typed input 形成一次 initial turn，固定 workflow/conversation identity 复用同一 session/loop，相关 `AgentResultMessage` 解码为 `TemplateSkillSubmission`；旧 `TemplateDistillationAgentInvoker` 对 `ReportingAgentRunner.run` 的包装删除。Characterization First 先因 bridge 模块缺失 RED，GREEN 后通过 monkeypatch 明确禁止 `ReportingAgentRunner.run` 与 `ReportWorkflowRunner.run`，连续两次调用均复用同一 session 并返回 typed result；focused `6 passed`，定向 Ruff、compileall、生产 import 扫描与 `git diff --check` 通过。本片刻意不解释 Recovery policy，不重新实现 correction、Max Token/Tool Slice continuation、No-progress 或 completed-result reuse；这些既有持久化语义仍是后续生产 Provider bridge 的必迁边界，因此当前 bridge 不进入公开 Binding。未新增 Hash/CAS、锁/lease、validator、Gate、依赖、attempt/replay policy 或公共接口。
- FA-03/M8.2c 在同一 Capability bridge 增加窄 `completed_result_loader` 边界：loader 提供已经过现有持久化边界验证的 typed `TemplateSkillSubmission` 后，bridge 只调用既有 `AgentExecutionService.recover_completed_result + AgentRecoveryDriver` 执行 `COMPLETED_TOOL_RESULT` policy，并在创建 Agent session 前返回，因此 Provider 调用为 0、session registry 为空。Characterization First 先因构造器缺少 loader 取得 `TypeError` RED，GREEN 后 Capability focused 与旧 Runner pre-session reuse 基准合计 `8 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片没有复制 `TaskAttemptStore.current → _same_recoverable_task → load_verified_result` 的既有 hash/terminal/identity/lease 校验；真实 loader 接线仍是下一边界。`RunToolResultIndex` 继续只负责 Tool 结果复用，不被误当作语义 task result。Correction、Max Token/Tool Slice continuation 与 durable No-progress 尚未迁入，不宣称恢复等价完成。未新增或扩大 Hash/CAS、锁/lease、validator、Gate、attempt/replay policy、依赖或公共接口。
- FA-03/M8.2d 将上述 loader 接到 Capability 既有真实持久化边界：新增 `runtime/completed_result_recovery.py`，由调用边界提供完整 `TaskCorrelation`，直接复用 `TaskAttemptStore.current` 与 `load_verified_result`，按旧 Runner 原有顺序匹配 semantic task、terminal status 和 AgentResult 的 task/run/agent/session identity；仅 completed typed result 进入中立 `recover_completed_result`，命中时 Provider/session 均为 0。旧 Runner 的 `_same_recoverable_task` 改为一行调用同一 Capability 纯比较函数，避免两份实现，排除字段仍仅为原有 attempt/lease 字段。真实文件持久化 Characterization 先因 helper 缺失取得 `ModuleNotFoundError` RED，GREEN 后 Capability 与旧 Runner recovery focused 合计 `9 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片没有生成、复制或扩展 Hash/CAS、锁/lease、attempt/replay policy；Correction、Max Token/Tool Slice continuation 与 durable No-progress 仍未接入该新 bridge，不能宣称 Provider 恢复等价完成。
- FA-03/M8.2e 将模板 Agent 首轮自然语言但未提交 typed result 的路径接入既有 `AgentExecutionService.execute_with_recovery + AgentRecoveryDriver`：Capability bridge 只把 `AgentResponse` 解释为 `NATURAL_LANGUAGE_WITHOUT_SUBMISSION`，构造声明式 policy 驱动的 `submission_correction` typed Prompt，并在同一 Conversation/Session/loop 中继续到 `TemplateSkillSubmission`；Provider session 只创建一次。Characterization First 先证明旧 bridge 返回 incomplete 的 RED，GREEN 后模板 bridge 与旧 Runner 两项 correction 基准合计 `11 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片未迁移 schema-invalid correction、Max Token/Tool Slice continuation 或 No-progress，也未新增 attempt、Hash/CAS、锁、Gate、validator、依赖或公共接口。
- FA-03/M8.2f 将模板 Agent 的既有 `AGENT_MAX_TOKENS_CONTINUATION_REQUIRED` marker 映射为 `RecoveryEventKind.MAX_TOKENS`，继续由同一 `AgentExecutionService.execute_with_recovery + AgentRecoveryDriver` 执行声明式 CONTINUE，并在原 Conversation/Session/loop 发送 Capability-owned `max_tokens_continuation` Prompt 后解码 typed result。Characterization First 先证明旧实现把 marker 误判为 natural recovery 的 RED，GREEN 后模板 bridge 与旧 Runner max-token/声明式恢复基准合计 `12 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。Schema-invalid 仍精确停在现有 `SubmitResultTool` 私有 callback 边界：通用 `ToolResult` 尚无 workflow/run/task/session 关联，Runtime service 也不订阅该事件，因此本片没有猜造关联或复制校验算法；Tool Slice、durable No-progress 与 correction persistence 仍未迁入。未新增 attempt、Hash/CAS、锁、Gate、validator、依赖或公共接口。
- FA-03/M8.2g 将模板 Agent 的既有 `AGENT_TURN_CONTINUATION_REQUIRED` marker 映射为 `RecoveryEventKind.TOOL_SLICE_BOUNDARY`，由同一通用 recovery loop 执行 CONTINUE，并在原 Conversation/Session/loop 发送 `tool_slice_continuation`；Capability Prompt 明确复用当前上下文和已完成 Tool result、不得重跑已完成工具。Characterization First 先证明旧 bridge 把 marker 误判为 natural recovery 的 RED，GREEN 后模板 bridge 与旧 Runner Tool Slice 基准合计 `12 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片刻意不复制旧 durable progress snapshot、No-progress 阈值、Hash/CAS 或 continuation limits；Schema-invalid、durable No-progress 与 correction persistence 仍未完成。未新增 attempt、锁、Gate、validator、依赖或公共接口。
- FA-03/M8.2h 新增业务中立 `runtime/typed_agent_turn.py`：统一 Agent session start/restore/reuse、`AgentResultMessage` 精确 terminal correlation、turn dispatch，以及 typed/error/response/status 到 `AgentInvocationOutcome` 的映射；它不认识 Capability 输入、Prompt、result model、Tool 或 recovery policy。模板 bridge 已改为组合该 primitive，删除自身重复的 session/correlation/dispatch/status 代码，同时保留 completed reuse、natural correction、Max Token、Tool Slice 的 Capability decode/observation。Characterization First 先因模块缺失 RED，GREEN 后 Runtime/Template/旧 Runner recovery affected `23 passed`，定向 Ruff、compileall、独立 import 与 `git diff --check` 通过。未新增 Gate、Hash/CAS、锁/lease、attempt、依赖或 HTTP/长期公共接口；Module/Final bridge 复用该 primitive 留给随后的机械收敛切片。
- FA-03/M8.2i 新增业务中立内部 `runtime/provider_agent_session.py`，把调用方已解析的 Provider、bus、tools、system prompt、loop config 与现有 observers/callbacks 原样交给具体 AgentLoop builder，并生成 `AgentExecutionService` 所需 session factory；它不认识 Reporting/Capability/Task/Schema，也不解释 recovery。现有 `ReportingAgentRunner` 的生产 `AgentLoop(**loop_kwargs)` 构造已机械改为组合该 factory，原 TaskAttemptStore、ProviderAdmission、IdentityLease、Recovery、Hash/CAS 与结果持久化全部留在原所有者。Characterization First 先因 factory 模块缺失 RED，GREEN 后 factory/生产接线/stable-session/max-token/tool-slice/no-progress/completed-result affected `7 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。未新增校验、重试、限制、Gate、锁、依赖或公共 HTTP/API。
- FA-03/M8.2j 将 4,190 行 Reporting collaboration Python Tools 与完整 `WorkflowMessageRouter` 从 Core 物理迁入 Distribution Capability，旧 `core/tools/reporting_collaboration_tools.py` 和 `core/reporting/message_router.py` 删除且不留 shim/re-export；`ReportingAgentRunner` 与测试消费者统一改用 Capability 唯一实现。Submit/WriteResultPart/ListResultParts/ReportBlocked/Peer/Gap、schema correction callback、Tool result persistence/reuse 的函数体未改。为避免独立 Capability import 经 `core`/`core.loops` convenience exports 反向急切加载 Reporting，两个 package initializer 改为保持同名 exports 的惰性 `__getattr__`，显式导入 smoke 保持原符号。Ownership/Router/Collaboration/Chapter Tool focused `111 passed`，workflow 文件 `59 collected`，定向 Ruff、compileall、旧路径/独立 import 与 `git diff --check` 通过。旧大测试文件的 13 个既有/并行 lint 债务未在本片越界清理；未新增或改变 validator、Recovery、Hash/CAS、锁、Gate、依赖或公共接口。
- FA-03/M8.2k 将 Reporting research Tool、ReferenceLibrary、Web backend、Evidence memory 与 KnowledgeContext 从 Core 物理迁入 Distribution Capability，旧 `core/tools/reporting_research_tools.py` 与 `core/reporting/research` 删除且不留 shim/re-export；旧 Runner 仅改为消费 Capability 唯一实现，搜索、缓存、Evidence/Source ledger 和网络后端函数体未重写。独立子进程 Characterization 证明 Capability research 组件导入时不会加载 `manyselves.core.reporting`，旧路径均不可发现；主线程 ownership/reference/tool 复核 `19 passed`，定向 Ruff、compileall 和 `git diff --check` 通过。未新增或复制 Hash/CAS、锁、validator、Gate、网络策略、依赖或公共接口。
- FA-03/M8.2l 补齐 standalone Template Runtime 的完整组合 Characterization：`TemplateDistillationWorkflowRuntime` 从文件定义经 `WorkflowCompiler → WorkflowRuntimeHost → TemplateDistillationAgentBridge → AgentExecutionService` 执行一次 typed Agent turn，完成 Run 并物化全部 14 个 Skill 文件；测试显式禁止 `ReportingAgentRunner.run` 与 `ReportWorkflowRunner.run`，证明该直接 Runtime 路径不借用 Legacy Runner。主线程独立复核 Template focused `12 passed`，定向 Ruff 与 `git diff --check` 通过。该证据使用 scripted Generic Agent session；`capability.yaml` 尚未公开 `distill-template-skill`，生产 Binding 也尚未提供 Provider/session composition，因此不宣称 Web/真实 Provider 已接线。未新增生产代码、Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.2m 新增 Capability-owned `runtime/template_tools.py`，统一组装 Template Provider 的 `inspect_document`、`write_result_part`、`list_result_parts`、`submit_result`、`report_blocked` 与对应 Provider-visible typed submission/part schema；`ReportingAgentRunner` 的 Template 分支改为薄调用同一 helper，仍由原 owner 注入 TaskCorrelation、recovery callback 和通用 completed Tool result reader。Capability Template `13 passed`，旧 Runner Template 精确对照 `9 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。既有 CAS 模板视图检查、ArtifactGateway、TaskAttemptStore、RunToolResultIndex 和 schema correction/recovery 算法没有复制或迁入 helper；该切片只完成 ToolRegistry 物理归属，生产 Binding/Provider session factory 与 schema-invalid Runtime correlation 仍未接线。未新增 Hash/CAS、锁/lease、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.2n 新增 Capability-owned `runtime/template_provider.py`，从 account-scoped `RuntimeServicesView` 复用同一 workspace、MessageBus、active Provider 和 Agent defaults，以既有 `ProviderAgentSessionFactory` 构造 `AgentLoop`，注入 Capability-owned Template 五工具并由 `TemplateDistillationAgentBridge/TypedAgentTurn` 执行 typed initial turn。Characterization 证明 loop 收到原始 Provider/bus/config/system prompt/ToolRegistry，连续两次调用只创建一个 loop/session，Conversation external session 与 typed result identity 保持一致，并由 runtime lifecycle 关闭同一 workflow session；Template focused `14 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片没有创建 ProviderManager，也没有复制 ProviderAdmission、TaskAttemptStore、RunToolResultIndex、ArtifactGateway、Hash/CAS、锁或 recovery 算法；当前是 Provider composition 前置能力，不是 recovery parity，生产 Web Binding、durable attempt/correlation、schema-invalid Tool event、usage/admission observer 仍待接线。未新增 Gate、依赖、Action Kind 或公共接口。
- FA-05/L1 删除生产零引用的 `runtime/agent_adapter.py` 与其 Legacy Reporting Runner 专用测试，不保留 adapter、alias 或 shim；其中两项真正中立的 Agent Conversation Characterization 已迁到 `test_agent_conversation.py` 并改由 Generic `WorkflowRuntimeHost` 执行，继续证明 Provider session 从 `AgentInvocationOutcome` 持久化及跨 Agent Conversation identity 拒绝。迁移前旧测试精确选择 `2 passed`，迁移后新 Host 测试 `2 passed`，定向 Ruff、compileall、旧符号/Runtime Reporting import 扫描和 `git diff --check` 通过。Generic 层架构测试现在只剩 `application/reporting_facade.py` 的旧 Runner import；未新增替代兼容层、Hash/CAS、锁、Gate、依赖或公共接口。
- FA-03/M8.3a 为 `aggregate-existing` 建立可编译、可由 Host 执行的 Capability 内部链：两个文件 Tool 从同一 Run frozen snapshot 读取五个 Markdown 或五个 structured module，复用既有完整性语义，写出 source manifest、integrity report 与 typed `AggregateEditorInput`；文件 Workflow 随后创建固定 `aggregate-existing` Conversation 并只调用新增的 `aggregate-editor` Agent，既不启动 Module/Cross/普通 Chief cohort，也不伪造 Final review/Delivery 完成。Characterization First 先因 runtime/workflow 缺失取得 `3 failed`，再证明 Host 在缺少 Agent adapter 的真实边界失败；GREEN 后 source/Compiler/Host focused `6 passed`，与 distill 合并复核 `12 passed`，Definition package、定向 Ruff、compileall 与 `git diff --check` 通过。当前仍未加入公开 entrypoints，且 Final review、Delivery 与真实 Provider Binding 留给后续切片。没有新增或扩大 Hash/CAS、锁/lease、validator、Gate、依赖或公共接口。
- FA-03/M8.3b 将 `aggregate-existing` Agent 结果改为 typed `AggregateExistingHandoff`，新增纯投影 Tool 与 `distribution-aggregate-existing-tail` 文件 Workflow，明确按 Aggregate → Final Chapter/Final Review → Delivery 顺序组合，同时跳过 Module、Cross 与普通 Chief cohort。Host Characterization 已执行 aggregate 与投影，并在现有 `prepare-final-chapter-cohort` Tool adapter 尚未接入的新边界真实失败；此时 Final action 为 failed、Delivery 为 pending、outputs 为空且没有生成报告产物，因此未伪造最终审查或交付完成。focused `9 passed`，定义索引除并行未提交的 full/module 文件外 `41 passed, 1 failed`，定向 Ruff、compileall、独立 import 与 `git diff --check` 通过；Capability 内部导入不加载 Core Reporting。下一切片必须迁入/绑定既有 Final/Delivery Tool 与 Agent runtime。没有新增或扩大 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3c 新增 Capability-owned `runtime/final_delivery_binding.py` 的首个确定性 Final boundary，迁入/组合 `prepare-final-chapter-cohort`、`prepare-current-final-chapter` 与 `final-chapter-initial-requires-agent` 三个 Tool 实现，不传 Runner callback、不导入 Core Reporting；aggregate Tool bundle 现在让 Host 完成 Final cohort prepare，并为 Chapter 1/3/4 生成 typed lane context，随后在第一个真实 `chief-editor-auditor` Agent adapter 缺失处停止。父 Run 仍保持 Final failed、Delivery pending、outputs 为空且不生成报告产物。Aggregate + 既有 Final cohort affected `13 passed`，定向 Ruff、compileall、生产 import 扫描与 `git diff --check` 通过。剩余 accept/complete/reduce、Final review round/revision/recheck 和三个 Delivery Tool 仍在现有 Core runtime，未宣称 Final/Delivery 完成。迁入的是既有章节/专项拆分与 typed envelope 语义，没有新增或扩大 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3d 新增 Capability-owned `runtime/final_agent_bridge.py`，直接组合中立 `AgentExecutionService`，将 `FinalChapterLaneInput` 发送为 Chapter 1/3/4 各一次 typed initial turn，并把相关 `AgentResultMessage` 解码为 `FinalChapterLaneFindingSubmission`；固定 conversation/session identity 由 Host 的既有 Conversation registry 与 Agent service 复用，不包装 `DeclarativeFinalChapterRuntime`、`ReportWorkflowRunner` 或 `ReportingAgentRunner`。Characterization 证明三个 Final Agent 均真实完成，Host 随后准确停在下一缺口 `accept-current-final-chapter-initial` Tool；Final action 仍 failed、Delivery pending、outputs 为空且没有报告产物。Aggregate focused `9 passed`，定向 Ruff、compileall、Capability 独立 import 与 `git diff --check` 通过。本片仅迁移一次 typed initial turn，尚未接 correction、Max Token/Tool Slice continuation、No-progress 或 completed-result reuse，因此不宣称 Final Agent Provider 恢复等价；未新增 Hash/CAS、锁/lease、validator、Gate、attempt/replay policy、依赖或公共接口。
- FA-03/M8.3e 将 Final initial 文件图中的三个确定性边界真正绑定到 Capability `FinalChapterTools`：`accept-current-final-chapter-initial` 校验并持久化 typed lane submission，`complete-current-final-chapter` 投影 terminal outcome，`reduce-final-chapter-cohort` 汇总 Chapter 1/3/4 并写出 `final-initial-aggregate.json`。这些步骤由 Workflow Host 的 Parallel/Subworkflow/Join 状态负责 same-run 完成分支复用，不复制旧 Runner 的 RecoveryState 记录。Characterization 从原 `missing tool adapter: accept-current-final-chapter-initial` 推进到下一真实缺口 `missing tool adapter: start-final-review-cycle`；三个 Agent/三个 lane/Join/reduce 均 completed，而 Final Review 父 action 仍 failed、Delivery pending、outputs 为空。Aggregate focused `9 passed`，定向 Ruff、compileall、Capability 独立 import 与 `git diff --check` 通过。未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3f 新增 Capability-owned `runtime/final_review_tools.py`，机械迁入 Final review cycle 的确定性首段：`start-final-review-cycle` 从初始 lane outcomes 建立 affected-only pending findings，`final-review-needs-round` 选择是否进入修订，`advance-final-review-round` 按既有最大轮次语义进入 revision 1。Host Characterization 用 Chapter 1 blocking finding 证明三步均 completed、pending chapter 仍为 1，并从原 missing start Tool 推进到下一真实缺口 `prepare-current-final-chief-revision`；Chief revision、recheck、Final completion 与 Delivery 均未伪造。Aggregate focused `9 passed`，定向 Ruff、compileall、Capability 独立 import 与 `git diff --check` 通过。未复制旧 Runner 控制流或 RecoveryState，也未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3g 将受影响章节的 Chief revision preparation 与一次 typed Agent turn 迁入 Capability：`FinalReviewTools` 按既有章节 section bodies、模块/跨模块来源投影、evidence refs、pending findings 与 revision identity 写出 `ChiefChapterLaneInput + TaskEnvelope`，Chief Skill 通过 `inline_context` 进入 Provider 可见 Prompt；新增 `FinalChiefAgentBridge` 组合业务中立 `TypedAgentTurn`，只保留 Chief Prompt 与 `ChiefChapterLaneRevisionSubmission` 解码。Characterization 证明 Chapter 1 修订 Agent 在原声明图中真实完成，Host 随后准确停在下一缺口 `accept-current-final-chief-revision`；没有伪造 accept/reduce、recheck、Final completion 或 Delivery。Aggregate focused `9 passed`，定向 Ruff、compileall、Capability 包导入扫描与 `git diff --check` 通过。该 bridge 尚未接 correction/continuation/No-progress/completed-result recovery，因此不宣称 Provider 恢复等价；未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3h 将既有 `FinalChapterAgentBridge` 的通用 Agent turn 机械收敛到业务中立 `TypedAgentTurn`：session start/restore、typed terminal correlation、dispatch 与 terminal/status 映射不再由 Final Capability 重复实现；Final bridge 只保留 `FinalChapterLaneInput` Prompt（含 lane specialization/inline Skill context）和 `FinalChapterLaneFindingSubmission` typed decode。Aggregate Characterization 前后均为 `9 passed`，Host/Final review 边界不变；定向 Ruff、compileall、直接实现扫描与 `git diff --check` 通过。该重构没有新增或扩大 recovery、Hash/CAS、锁/lease、validator、Gate、依赖或公共接口。
- FA-03/M8.3i 将 Final Chief revision 的 accept/complete/reduce 三个真实边界迁入 Capability：accept 保留 run/subject/chapter/revision identity 与 revision-1 finding response coverage，读取声明 task/revision 目录内的 Markdown part bodies 并持久化 lane submission；Cohort/Join 后 reducer 组装更新后的 `EditedReportSubmission`、写出 `chief-rN.json` 并更新 `chief_candidate_ref` 与 review subject。Characterization 证明 active Chapter 1 lane、Join、Reducer 均 completed，`assessment_background` 已由真实 part 更新，Host 随后准确停在 `prepare-current-final-recheck`；Recheck/Final completion/Delivery 未伪造。Aggregate focused `9 passed`，定向 Ruff、compileall、Capability import 与 `git diff --check` 通过。revision≥2 的旧 Runner RecoveryState aggregate 文件没有 Capability 下游消费者，新路径由 Host WorkflowState、`chief-rN.json` 与 `chief_candidate_ref` 承接，故未复制该旧控制流产物；没有新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3j 将受影响章节的 Final recheck preparation 与一次 typed Auditor turn 迁入 Capability：生成 phase=recheck 的 `FinalChapterLaneInput`，只内联 changed section bodies，并机械保留旧 contract 的 `unchanged_section_sha256`、required findings 与 revision responses；完整 `final-auditor` Skill 和 chapter specialization 一起进入 Provider 可见 Prompt，初始 Final lane 同步补齐完整 Skill。`FinalChapterAgentBridge` 继续组合 `TypedAgentTurn`，按 contract phase 分别解码 finding 或 verdict typed submission。Characterization 证明同一 Final Conversation/Session 完成 recheck verdict Agent，Host 随后准确停在 `accept-current-final-recheck`；accept/reduce、Final completion 与 Delivery 未伪造。Aggregate focused `9 passed`，定向 Ruff、compileall、Capability import 与 `git diff --check` 通过。SHA-256 仅是原有 Auditor contract 的未修改小节投影，本片未新增或扩大 CAS、锁、Gate、校验机制、依赖、Action Kind 或公共接口。
- FA-03/M8.3k 将 Final recheck accept/complete/reduce 与 `complete-final-review` 迁入 Capability：accept 保留 verdict run/chapter/coverage/required finding identity，Cohort reducer 更新 verdict history、latest refs 与 unresolved/new findings；resolved 路径写出既有 `ReviewCompletionRecord`、`FinalAuditSnapshot` 和 reporting-state handoff refs，unresolved/new finding Characterization 证明同一文件图回到下一 revision round。Aggregate resolved 路径已真实完成 Final Review，并准确停在 Delivery 的 `prepare-render-delivery`；Render/Publish/Delivery 未调用或伪造。Aggregate + Definition focused `52 passed`，定向 Ruff、compileall、Capability import 与 `git diff --check` 通过。未复制 RecoveryState，也未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3l 将已有 Capability-owned Delivery Tools 接入 `distribution-report-delivery.yaml` 的真实 Generic Host 链：`prepare-render-delivery` 生成当前 Run 的最终 Markdown、DOCX、来源索引和 Render 产物，`publish-materialize-delivery` 物化公开文件与 delivery receipt，`complete-delivery` 写入 completion 和序列化 `output_artifacts`。Aggregate-existing 的文件 Tail 现在从 Final Review 继续完成 Render → Publish → Complete，Characterization 同时证明没有绕回 Cross 或普通 Chief cohort；主线程复核 aggregate 精确路径与 Delivery Tool 选择 `4 passed`，定向 Ruff、Capability 独立导入、compileall 和 `git diff --check` 通过。该切片使用 scripted Agent 证明 Host/文件产物语义，尚未进行真实 Provider/浏览器测试；未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3m 将完整 Tail 组合能力接入 `AggregateExistingWorkflowRuntime` 自身：Runtime 可按文件 `distribution-aggregate-existing-tail` 编译并执行 Aggregate → Final Chapter → Final Review → Delivery，并通过注入的通用 Agent invoker map 绑定 aggregate editor、chief editor 与 final auditor；默认内部 aggregate workflow 仍保留给现有窄调用。Characterization 证明 Final Chapter 1 的 initial/recheck 使用同一 Conversation 且 recheck 复用 initial 返回的外部 Session，当前 Run 同步生成 Markdown、DOCX、receipt、completion 与 output artifacts，同时没有执行 Module、Cross 或普通 Chief cohort。主线程精确选择 `2 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。该切片完成的是 Capability Runtime 直接构造路径，生产 `DistributionReportingRuntimeBinding` 尚未构造此 Runtime，且 Agent 为 scripted，并未宣称 Web/真实 Provider 已迁移；未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.3n 新增 Capability-owned `AggregateEditorAgentBridge`，复用中立 `TypedAgentTurn/AgentExecutionService` 完成 `AggregateEditorInput` Provider-visible Prompt、typed `EditedReportSubmission` 解码与 Conversation/Session 复用；完整 `AggregateExistingWorkflowRuntime` Tail Characterization 已把 aggregate-editor 从自定义 scripted invoker 替换为该 bridge，并显式禁止 `ReportingAgentRunner.run` 与 `ReportWorkflowRunner.run`，Final/Delivery 仍完成且生成当前 Run 全部文件。主线程独立复核 Aggregate focused `12 passed`，定向 Ruff、compileall、Capability import 与 `git diff --check` 通过。该 bridge 目前只覆盖 typed initial turn，Final roles 仍使用各自已抽取边界或 scripted Agent，生产 Web Binding/真实 Provider factory 与恢复持久化尚未接线，因此不宣称 Provider parity；未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4a 新增 `full-report.yaml` 与 `module-report.yaml` 两个公开命名的根文件 Workflow，以及 Preparation/Readiness/Module 之间的三个纯 typed 投影 Tool、模块结果 attachment Contract 和公共 Output Definition。full-report 文件拓扑已声明 initialize → Preparation → Readiness → Module Cohort → Reporting Tail → publish/end；module-report 声明相同前半段但不含 Tail/Cross/Chief/Final/Delivery。新增 `specialize_module_cohort_workflow` 可在编译前从 Parallel branches、Join/Reducer inputs、state 和 task refs 中物理移除未选模块，Characterization 证明选择 2.4/2.5 后图中没有 2.1。文件缺失先取得 `2 failed` RED；GREEN 后新/既有 entrypoint focused `14 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。当前根图只有在注册既有 module/cross/chief/final definition specializers 后才被 Compiler 接受；module target specialization 函数尚未接入生产 Binding，且两个 workflow 尚未加入 Capability entrypoints，因此不宣称公开运行完成。未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4b 新增 Capability-owned `PublicReportingWorkflowRuntime`，负责加载定义、注册已有 specialization、按 `ReportRequest.target_modules` 在编译前替换 Module Cohort、保存 Resolved Plan/Run state，并以一次 `WorkflowCompiler + WorkflowRuntimeHost` 执行公开根图。`module-report(target=2.4)` 的实际 Plan/Subworkflow 已证明只含 2.4 lane；Host 真正完成 Preparation、Readiness 与 reporting-state 构造后抵达 2.4 Author Agent 边界，其他模块 Agent 未出现。单模块图移除 Parallel/Join，并把唯一分支直接连接 Reduce，不执行五个再丢弃。缺失 Tool 只是不注册，由 Host 原生 missing-adapter 错误表达；已删除自定义 missing callback/stub 与 request/workflow mismatch 额外判断。新 Runtime focused + 根定义测试 `14 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。full-report 仍准确停在现有 Tail specialization 未迁入 Capability 的边界，Runtime 尚未接公开 Capability Binding，不宣称完整运行或输出。未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4c 新增 Capability-owned `runtime/module_agent_bridge.py`，直接组合中立 `AgentExecutionService`，把选中 Module Author 的 `DeclarativeModuleRuntimeLaneContext` 发送为一次 typed initial turn，并将相关 `AgentResultMessage` 解码为既有 `ModuleSubmission`/`DeclarativeModuleAuthoringAgentResult`。`PublicReportingWorkflowRuntime` 只在注入的 module runtime 已提供 neutral execution/session factory 时替换 specialist invoker，不新增构造参数或长期公共接口。Characterization 证明 `module-report(target=2.4)` 的实际图仍只有 2.4，Host 已完成 Author Agent 并把 typed module 写入 lane state，随后准确停在下一真实缺口 `accept-current-module-authoring`；没有调用 Legacy/Declarative Runner。公开根与定义 affected `15 passed`，定向 Ruff、compileall、Capability 独立 import 与 `git diff --check` 通过。本片不含恢复循环或真实 Provider factory，未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4d 新增 Capability-owned `runtime/module_lane_tools.py`，机械迁入 Author accept 与紧邻的 authored 路由谓词：验证 typed module identity，按原路径写入 `Work/runs/<run>/modules/<module>-r<revision>.json`，并把 submission 放入 lane 与 reporting `specialist_submissions`。`PublicReportingWorkflowRuntime` 用 Capability 实现覆盖旧 module runtime 的同名 Tool；Characterization 使用真实 `DeclarativeModuleAuthoringPreparation + TaskEnvelope.inline_context`，证明 Provider-visible Prompt 保留 Author Skill、2.4 typed result 被持久化，Host 随后准确停在尚未迁移且包含真实 preflight/envelope 语义的 `prepare-current-module-review`。没有用简单 status 变更伪造 Reviewer 分支。公开 runtime/entrypoint affected `16 passed`，定向 Ruff、compileall、Capability 独立 import 与 `git diff --check` 通过。未新增 Hash/CAS、锁/lease、validator、Gate、RecoveryState、依赖、Action Kind 或公共接口。
- FA-03/M8.4e 将 `ModuleAuthoringAgentBridge` 的通用 Agent turn 机械收敛到业务中立 `TypedAgentTurn`：动态 workflow identity 下的 session start/restore、typed terminal correlation、dispatch 与 terminal/status 映射不再由 Module Capability 重复实现；Module bridge 只保留完整 lane context/`TaskEnvelope.inline_context` Prompt 与 `ModuleSubmission` typed decode。公开 module runtime Characterization 在并行 Reviewer preparation 产物完成后通过，定向 Ruff、compileall、直接实现扫描与 `git diff --check` 通过。该重构没有新增或扩大 recovery、Hash/CAS、锁/lease、validator、Gate、依赖或公共接口。
- FA-03/M8.4f 将 Module initial Reviewer 的真实 preparation 边界迁入 Capability：纯确定性 `review_preflight.py` 从 Core 物理迁移且旧文件删除，`module_review_preparation.py` 生成并持久化结构报告、preflight、typed `ModuleReviewInput` 与完整 Reviewer `TaskEnvelope`，保留稳定 `module-auditor-<module>` session key、全部 initial review 约束、用户补充投影、Evidence/Knowledge 与 auditor Skill inline context。`module-report(target=2.4)` Characterization 证明只准备 2.4，Host 随后准确停在下一真实声明缺口 `module-review-preflight-needs-revision`；Reviewer Agent、preflight author correction 与 revision/recheck loop 尚未伪造。公开 Runtime + 旧 preflight affected `30 passed`，定向 Ruff、compileall、旧路径/import 扫描与 `git diff --check` 通过。没有新增或扩大 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4g 将通过 preflight 的 Module initial Reviewer 路由与一次 typed Agent turn 接入 Capability：新增纯谓词 `module-review-preflight-needs-revision`/`module-review-requires-agent`，新增 `ModuleReviewerAgentBridge` 组合 `TypedAgentTurn`，使用稳定 `evidence-auditor / module-auditor-<module>` Conversation/Session，把完整 typed `ModuleReviewInput` 与 auditor Skill 发送到 Provider 可见 Prompt，并解码 `ModuleReviewFindingSubmission`。`PublicReportingWorkflowRuntime` 仅在既有 neutral execution/session factory 可用时注入该 invoker。Characterization 证明选定 2.4 的 Author→Reviewer 都真实执行，Host 随后准确停在 `accept-current-module-review`；另证明 preflight failure 谓词路由到未迁移的 Author correction 边界，不伪造 correction/accept/revision/recheck。公开 runtime focused `6 passed`，定向 Ruff、compileall、Core/hash/CAS/lock 扫描与 `git diff --check` 通过。未新增 recovery、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4h 将 Module initial Reviewer accept 与紧邻路由迁入 Capability：校验 typed submission、完整 coverage、finding ID 唯一性/前缀/小节范围，持久化 `findings-r0.json` 与 `progress.json`；无 finding 时生成 Review completion 与 `Outputs/Modules/<id>.md`，有 blocking/advisory finding 时保留 pending 并路由到声明式 Author revision 边界。既有 findings artifact 的 JSON equality 语义被机械保留：同内容 same-run replay 可复用、不同内容不得覆盖；没有扩大到其他产物，也未引入 Hash/CAS/锁。Characterization 证明无 finding 的 Host 停在 `complete-current-module-lane`，有 finding 的 Plan 指向 `prepare-current-module-revision`，且 duplicate IDs/不同内容覆盖被拒绝；Author revision/recheck/Main exception 未伪造。公开 runtime focused `7 passed`，定向 Ruff、compileall、Core/hash/CAS/lock 扫描与 `git diff --check` 通过。未新增 recovery、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4i 完成 selected-module 无 finding 的声明式闭环：Capability-owned `complete-current-module-lane` 只把已 reviewed/completed 且已有当前模块 Review completion ref 的 lane 投影为 completed，`reduce-module-cohort` 只归并 specialized Plan 实际传入的模块，根 attachment 保留 typed specialist submission 与 completion refs。`module-report(target=2.4)` 现在由同一个 Generic Host 完成 Preparation → Readiness → 2.4 Author → initial Reviewer → Review completion → Cohort reduce → Publish/End，Characterization 保留 Reviewer envelope、稳定 session、Skill、typed submission 和产物断言，并证明其他模块未执行。主线程 completion/cohort/Host 精确选择 `4 passed`，定向 Ruff、compileall、Capability import 和 `git diff --check` 通过。finding 后的 Author revision/recheck/Main exception 仍是后续真实缺口；未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4j 完成 selected-module finding 的声明式 revision/recheck 闭环：Capability-owned Author bridge 按 task output contract 解码 typed `ModuleRevisionSubmission`，Revision Tools 构造/接受明确 patch，Recheck Tools 构造既有 `ModuleReviewInput(phase=recheck)` 并由原 `module-auditor-<module>` Conversation/Session 返回逐 finding verdict；resolved 路径写出 verdict、completion 与模块输出，open 或 new finding 路径保留 pending 并回到下一 Author revision。Characterization 证明 2.4 initial Author/Reviewer 各一次、revision Author 一次、同一 Auditor 两轮且 Session 相同，Host 不重放已完成 action；另覆盖 unresolved+new finding 不得完成。主线程 Public Runtime `12 passed`，旧 revision/recheck 对照 `4 passed`，定向 Ruff、compileall、Capability 独立 import 与 `git diff --check` 通过。审计时发现草稿漏掉既有 Claim scope 约束，新增 RED Characterization 后将旧 `_module_recheck_delta` 机械抽为 Capability 唯一实现，Core 仅保留 `ReviewLifecycleError` 转换；其中 SHA 只保留原 Provider-visible compact-delta 对未变 scope 内容的表达，不是新 CAS、运行门禁或重放判断。Preflight revision 与 Main exception 仍待后续切片；未新增锁/lease、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4k 新增 Capability-owned `CapabilityModuleRuntime` composition，只组合已经迁移的 lane start/cohort、review/revision/recheck、author-exception pass-through、deferred/status predicates 与 complete/reduce 端口，不从 Core 借回调。真实 `PublicReportingWorkflowRuntime(module_runtime=CapabilityModuleRuntime(...))` Characterization 证明编译图与 implementation map 已注入这些端口，并且刻意不注册尚无等价实现的 `prepare-current-module-authoring`；临时 workspace 的 Generic Host 实际走过 initialize → Preparation → Readiness → build reporting state → cohort → lane start/route 后，精确停在 `missing tool adapter: prepare-current-module-authoring`。Module composition `3 passed`、既有 Public Runtime `12 passed`，定向 Ruff、compileall、Capability import 与 `git diff --check` 通过。旧 Author preparation 仍依赖 module dispatch/Knowledge/Skill refs、planned TaskEnvelope、完整 Tool list、result-part continuation/resume、用户补充/evidence policy 及既有 context digest；本片已撤回简化草稿，未复制或弱化这些 Provider-visible 语义，也未新增 Hash/CAS、锁/lease、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4l 新增 Capability-owned `runtime/reporting_tail_runtime.py`，在 Definition Registry 内注册 full-report 所需的 5 个 Cross owner、3 个 Chief、3 个 Final chapter 及 6 个 Final revision/recheck 参数化子工作流，并组合已迁移的 Final/Delivery Tool bindings；不导入 Core Reporting、不调用 Legacy Runner，也不为尚未迁移的 Cross lifecycle 添加 stub。Characterization First 先证明原 `full-report` 编译在 `missing workflow: distribution-cross-owner-2.1-pipeline` 失败，组合后 Generic Host 已成功编译并进入 `distribution-reporting-tail`，准确停在第一个真实缺口 `missing tool adapter: prepare-cross-owner-cohort`，同时 Final/Delivery Tool 已在同一完整 Plan 的 Tool map 中。Tail + Public Runtime focused `14 passed`，定向 Ruff、compileall、Capability import 与 `git diff --check` 通过。Cross Agent/lifecycle、Chief Agent composition 与完整 full-report 执行仍待接线；未新增 Hash/CAS、锁/lease、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4m 新增 Capability-owned `runtime/cross_owner_composition.py`，把 Cross YAML 的 cohort、owner initial、Author revision、owner-local Auditor、recheck、Main exception 与 completion Tool ID 映射到一个注入的 Capability lifecycle port，并直接暴露该 owner 已组成的 Agent invokers；缺失端口不注册，继续由 Generic Host 在真实 action 处失败，不生成 no-op。`ReportingTailComposition` 现在可选接收该内部 port，同时保持无 port 时仍精确停在 `prepare-cross-owner-cohort`。Characterization 验证关键 Tool 与 Agent map 注入、无 Core Reporting import；focused `4 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。曾评估把 7,500 行混合 `review_lifecycle.py` 与 `chapter_parallel.py` 整体迁入 Capability，但因其同时包含 Module/Final 生命周期并依赖 Legacy runner/service、TaskAttemptStore/RecoveryStateStore 而撤回，未把原 SCC 换目录冒充解耦；完整 Cross lifecycle/Provider/recovery 仍待窄迁移。未新增 Hash/CAS、锁/lease、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4n 将完整 Module Author preparation 抽成 Capability 唯一实现 `runtime/module_authoring_preparation.py`：共享 dispatch、Module Knowledge/global Knowledge、14 份 Template Skill 边界、完整 Author `TaskEnvelope`、16 工具、用户补充/evidence policy、draft revision、saved/rewrite result parts 与 same-run typed submission resume 均在 Capability 生成；`CapabilityModuleRuntime` 绑定 prepare/resume 并传递 account global root，旧 `ReportWorkflowRunner` 六个同名 helper 仅保留薄错误转换/调用。fresh/resume Characterization 与旧 Provider-visible envelope 精确对照，真实 `PublicReportingWorkflowRuntime(module_runtime=CapabilityModuleRuntime(...))` 已替换测试 boundary runtime，由一次 Generic Host 完成 Preparation → Readiness → dispatch/Knowledge/Skill → Author → initial Auditor → selected-module completion；Author Prompt 证明 inline Author Skill 与完整 Tool list 存在。Module/Public/Legacy global Knowledge affected `18 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。`artifact_sha256` 与 `authoring_context_sha256` 仅机械保留既有固定 Skill source 清单和同 Run draft-context/result-part 恢复语义，不是新增 CAS、锁或执行 Gate；未新增依赖、Action Kind 或公共接口。新 Author/Reviewer bridges 仍是 initial-only，recovery parity 留在后续切片。
- FA-04/A1 新增业务中立、内部只读 `RuntimeServicesView`，只投影当前 account-scoped `RuntimeHost` 已拥有的 workspace、bus、active provider、Agent defaults 与 global knowledge root；active provider 优先复用现有 Main loop 对象，无 Main loop 时只读现有 ProviderManager selection，无 provider/workspace 时保留当前 degraded mode 的 `None`，不创建第二个 ProviderManager或选择策略。`manyselves.application` convenience exports 改为惰性加载，保持显式 `from manyselves.application import RuntimeHost/RuntimeFacade/ControlLeaseService`，同时让 services view 独立 import 不再反向加载 `core.reporting`。Characterization First 先因模块缺失取得 `3 failed`，GREEN 后 services + RuntimeHost affected `6 passed`，定向 Ruff、compileall、export smoke、独立 import 与 `git diff --check` 通过。该 view 尚未接 Capability Binding；未新增 HTTP/YAML/业务公共接口、Tool/Recovery 语义、Hash/CAS、锁/lease、Gate 或依赖。
- FA-04/A2 将五种用户操作收敛为 Capability 文件入口 `full-report`、`module-report`、`aggregate-existing`、`render-existing`、`distill-template-skill`，每个根 Workflow 只公开用户可填写的 Pydantic/JSON Schema；Host-owned `run-id` 不进入表单，Aggregate 与 Template 由两个 Capability-owned pure Projection Tool 把 `{run_id, request}` 转成已有内部 typed task contract，Template 固定十四 Skill ID 也不暴露给用户。Template standalone Runtime 同步改为从 application command identity 分配 Run，并由 Generic Host 执行 Projection → Preparation → Conversation/Agent → Materialization；未使用占位 Run ID。公开入口/Definition/Template/Aggregate/架构 focused `72 passed, 3 xfailed`，定向 Ruff、compileall 与 `git diff --check` 通过；架构矩阵中的“五种操作均为文件声明入口”已转为正式通过。生产 `DistributionReportingRuntimeBinding` 尚未构造这五个 Runtime，因此本片不宣称 Web/真实 Provider 已接线；未新增 Hash/CAS、锁/lease、validator、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4o 新增 Capability-owned `runtime/module_provider.py`，从 account-scoped `RuntimeServicesView` 组合 Module Author/Reviewer 的声明工具、`ProviderAgentSessionFactory`、`AgentExecutionService` 与 typed bridges；同一 Host Conversation 连续调用复用同一 Provider Session，Provider、bus、Agent defaults、system prompt、ArtifactGateway、TaskCorrelation 与结构化纠正 callback 均由现有 owner 注入。主线程额外修复 `PublicReportingWorkflowRuntime` 对显式 Provider invoker 的覆盖：已有可调用 invoker 必须原样进入 Host，不能再替换为没有 Provider Tool composition 的裸 bridge。Module focused `9 passed`，Provider session affected `1 passed`，定向 Ruff、compileall、Capability import 与 `git diff --check` 通过。本片刻意不把未实际消费的 RunToolResultIndex/usage observer 挂成 registry 元数据；durable Tool-result reuse、Provider attempt observers、completed-result/no-progress/continuation 仍是后续真实接线，不宣称 recovery parity。未新增 Hash/CAS、锁/lease、Gate、依赖、Action Kind 或公共接口。
- FA-03/M8.4p 新增 Capability-owned `runtime/cross_owner_runtime.py` 的 Cross initial/no-finding 垂直切片：从上游五个 typed Module submission 和既有关系 artifact metadata 生成五个 `CrossOwnerInput + TaskEnvelope`，每个 owner 通过 `TypedAgentTurn` 使用独立稳定 Conversation 并复用外部 Session；typed initial acceptance 可复用同 Run 已持久化结果，无 finding 时生成五 lane completion、Cross completion 与 decision pack。Tail Generic Host 已完成五 owner Parallel/Join/Reduce，并准确停在下一真实缺口 `prepare-chief-chapter-cohort`；focused `8 passed`，定向 Ruff、compileall、Capability 独立 import 与 `git diff --check` 通过。`CrossOwnerInput.related_module_sha256` 是既有 Provider contract 必填字段，本片不计算 digest、没有新增 hash/CAS 校验；因此真实 full-report 上游尚缺既有 `module_artifact_refs` 接线，当前 Host 探针显式注入该 metadata。finding→Author revision→本地 Auditor→Cross recheck/Main exception 也尚未迁移，initial bridge 仍不宣称 recovery parity。未新增锁/lease、Gate、依赖、Action Kind 或公共接口。
- FA-04/A3 为 `PublicReportingWorkflowRuntime` 补齐通用应用接口：`start` 将公开 user-only Schema 输入投影为已有 `ReportRequest`，以 application command identity 分配 Host Run ID；`get_run/get_outputs/get_cost` 直接读取通用 `WorkflowStateStore` 与既有 usage ledger，向 FastAPI/React 投影状态、WAITING 输入、声明值和交付 artifact，不再需要 Reporting snapshot 才能查询 file-defined full/module Run。Characterization First 分别先取得 `AttributeError: start` 与 `AttributeError: get_run` 的预期 RED；GREEN 后 public runtime focused `14 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。当前生产 `DistributionReportingRuntimeBinding` 尚未构造该 Runtime，`provide_input` 的通用嵌套 WAITING/resume 也尚未接线，因此不宣称 Web 入口或 same-Run 恢复完成。未改 Kernel/Compiler/Host，未新增 Hash/CAS、锁/lease、Gate、依赖、Action Kind 或长期公共接口。
- FA-03/M8.4q 将 Module Provider 缺失的基础 Tool 物理迁入 Capability-owned `runtime/module_provider_tools.py`：`InspectImageTool`、`CalculateTool` 与四个 indexed artifact reader 现在只有一份实现，Capability composition 可按声明组装 `inspect_image/calculate/open_artifact/search_text/open_tool_result`；旧 `ReportingAgentRunner` 删除重复类体并薄导入同一实现。artifact gateway/access 与既有 `RunToolResultIndex` 由当前 owner 显式注入，completed Tool result 继续直接调用原 `lookup/record`，本片未修改或复制其持久化/校验机制。Characterization First 先因 Capability builder 缺少 `artifact_access/result_index` 端口取得预期 RED；GREEN 后 Module focused `10 passed`，旧 Reader/artifact 受影响选择 `5 passed`，主线程复核 Module+旧 Reader 选择 `13 passed`，定向 Ruff、compileall、唯一类定义/独立 import 扫描与 `git diff --check` 通过。Provider attempt、Agent completed-result 与 no-progress 仍未在该 composition 接齐；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-04/A4 为 Public full/module Runtime 接入同一 Run 的通用 WAITING 恢复：`provide_input` 读取持久化 `ResolvedPlan + WorkflowState`，从 plan definition snapshots 恢复 Contract Registry，把 `waiting_input.path` 与 `subworkflow_plans` 原样交给既有 `resume_waiting_input`，随后仍由同一个 `WorkflowRuntimeHost` 执行恢复后的状态；start/resume 共用 `_execute_state`，没有 Capability 专属恢复分支或 Kernel Action。新 Characterization 只验证 Public Runtime 的 generic resume 接线，嵌套 Subworkflow 与 Parallel sibling 保留/不重放继续由既有 Host Characterization 覆盖；实现前因 resume 接线不存在取得预期 RED。GREEN 后 public runtime + 两项嵌套 Host focused `17 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。应用协议/route 当前仍是同步 `provide_input`，生产 Binding 尚未构造 Public Runtime，所以下一片必须把 async continuation 贯通到通用 Facade/FastAPI；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-04/A5 将通用 `CapabilityRuntimeBinding.provide_input`、`WorkflowProjectionFacade` 与 FastAPI Run input route 贯通为 async；Parameter Adjustment 删除 thread/`asyncio.run` 协程桥，直接 await 同一 Generic Host，Reporting/Render/Template adapters 同步采用统一协议，HTTP request/response Schema 不变。Characterization First 将 synthetic Binding 改为 async 后，旧 Facade 因未 await 得到 `TypeError: coroutine object is not subscriptable` 的预期 RED；GREEN 后 application/parameter/web projection focused `17 passed`，两项真实 HTTP input affected `2 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。受影响投影 Characterization 同步按当前五个公开文件入口改为 `full-report`，不再把内部 `distribution-reporting` 子流程误标为 runnable。生产 Distribution Binding 在本片仍只对 render await file Runtime，其他分支仍暂时调用 Legacy adapter，留给后续 composition；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或外部 HTTP 接口。
- FA-03/M8.4r 移除 Module Provider composition 的假依赖：`CapabilityModuleRuntime.agent_session_factory` 与 `build_module_provider_composition` 现在允许省略，因为显式 `ModuleProviderRuntime` invoker 已在每个 prepared TaskEnvelope 上构造真实 Provider session factory，且 Public Runtime 对可调用 invoker 必须原样保留。Characterization First 去掉占位 lambda 后，旧 builder 先以 `missing required keyword-only argument: agent_session_factory` RED；GREEN 后 composition/provider-invoker focused `2 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。保留旧测试对显式注入 session factory 的验证，未改变 Agent/Conversation/Session 生命周期；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-03/M8.4s 将 Reporting 的 artifact access compiler 物理迁入 Capability-owned `runtime/artifact_access.py`，旧 `core/reporting/capabilities.py` 删除且不保留 shim；nested ref/photo 收集、opaque ref、workspace/cross-run、template isolation、Provider-visible capability 与 Tool scope 规则均保持唯一原实现。旧 `core.reporting.config.ConfigurationError` 现在引用 Capability 的同一异常类，类身份未改变；访问编译器用最小结构协议替代对 Core `AgentDefinition` 的反向依赖。Characterization First 先因 Capability 模块不存在取得预期 RED；ownership/affected focused 由 Luna 验证 `27 passed`，主线程交叉选择 `17 passed`，定向 Ruff、compileall、旧 import/唯一实现扫描与 `git diff --check` 通过。未新增或扩大 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-03/M8.4t 将项目内容快照的唯一实现机械迁入 Capability-owned `runtime/content_snapshot.py`，`ReportingService.snapshot_content` 仅保留薄兼容委托。原 `ContentAddressedStore.ingest_file/issue_trusted_handle/link_trusted_view`、SHA-256 immutable identity、项目写 lease、staged `.cas-view`、`os.replace` 与失败清理语义原样保留，用于 Preparation 固化同 Run 的 Provider-readable 输入，不新增第二套文件复制或校验机制。Characterization 覆盖首次 ingest、相同 target、replace-existing-view 与 Service 委托；Luna focused `5 passed`，主线程与 artifact access 交叉选择合计 `17 passed`，实现定向 Ruff、compileall 与 `git diff --check` 通过。测试文件全文件 Ruff 仍有两项切片外既有规则告警，本片未修改；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-03/M8.4u 将 Module Provider 的 artifact composition 从测试注入推进到真实 per-Task 组合：每个 prepared `TaskEnvelope + Agent + Conversation/Session` 都从 workspace root `ArtifactGateway` 生成既有 scoped grant，交由 Capability `compile_agent_access` 读取 typed `input_contract_ref` 并编译声明 Tool scope，同时按 `envelope.run_id` 使用独立的原 `RunToolResultIndex`；Provider loop 与 Tool builder 接收同一个 scoped gateway/access/index。显式 dependencies 仍可供 focused/offline owner 注入，但生产无注入路径不再得到空 artifact 能力。Characterization First 在旧实现取得 `artifact_gateway is None` 的预期 RED，GREEN 后两个不同 Run/Task 的 grant、result index 与 readable refs 均隔离；Luna及主线程 focused 各 `11 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片未触碰 TaskCorrelation/recovery，也未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-03/M8.4v 新增 Capability-owned `runtime/chief_runtime.py` 的 Chief initial 垂直切片：按 Chapter 1/3/4 的声明 scope 生成 `ChiefChapterLaneInput`、bounded source projection、完整 `TaskEnvelope`、三项 result-part Tool、既有 template Skill inline context；`ChiefChapterAgentInvoker` 组合中立 `TypedAgentTurn/AgentExecutionService`，使用稳定 `chief-chapter-<id>` Conversation/Session 解码 typed submission。accept/complete/reduce 持久化 lane result parts 与初始 `chief-r0` candidate，approved Module 正文由 typed `ModuleSubmission.markdown` 确定性组装。Characterization First 先因模块不存在取得 RED，并逐字段与旧 source projection 对照；Capability focused `3 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。当前尚未把该 runtime 注入 tail，旧 Core compatibility 实现仍待删除；recovery-store lane/aggregate 记录、Chief revision/recheck 与 Provider Tool composition 也未在本片宣称迁移。未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-03/M8.4w 将 Chief initial runtime 接入 Capability `ReportingTailComposition`：可选 `chief_runtime` port 在存在时把六个声明 Tool 实现与原 `chief-editor` invoker 注入完整 full-report Plan，不存在时仍保留 Generic Host 的真实缺口，不生成 stub、fallback 或 Legacy 调用。Characterization First 先因构造器不接受该 port 取得 RED；GREEN 后验证三个参数化 Chief lane 的全部 Tool ID 都有绑定、invoker 身份原样传递，并由独立子进程证明 composition 不加载 `manyselves.core.reporting`。Tail focused `10 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。生产 Provider session/tool factory、recovery-store 与 Final composition 仍待后续切片；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-03/M8.4x 新增 Capability-owned `ChiefProviderRuntime/ChiefProviderComposition`：用 account-scoped `RuntimeServicesView`、中立 `ProviderAgentSessionFactory/AgentExecutionService`、按 `TaskEnvelope` scope 编译的 Artifact access、原 `RunToolResultIndex` 与声明的 `write/list/submit` 三项 collaboration Tools 驱动 Chief initial Agent。真实 `SubmitResultTool` Characterization 首先因 Action ID 与 TaskEnvelope terminal identity 不同且结果为 `AgentResult` wrapper 得到 timeout RED；修复后 Runtime 只接受调用方显式 terminal sender/task/attempt，Capability 只读取既有 wrapper 的 typed `payload`，同一 Conversation/Session 连续两轮真实 Tool 链均完成且 Provider loop 只创建一次。Chief/provider focused `5 passed`、Tail affected `10 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。未修改 Kernel/Action，未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖或公共接口；Chief revision/recheck、recovery-store 与最终生产 Binding 仍待后续切片。
- FA-03/M8.4y 将 Module Author/Revision、Reviewer/Recheck 与 Final Auditor/Recheck bridges 对齐同一真实 Tool terminal：Host request 继续保留 file Action ID，terminal 精确使用各准备阶段 `TaskEnvelope.agent_id/task_id` 和当前无 correlation 的空 attempt；decoder 只消费既有 `AgentResult.payload`，不再把结果文件误当裸 submission。Characterization First 把 scripted fixtures 改为真实 wrapper/envelope identity 后旧 bridge 取得 RED；GREEN 后 Module focused `12 passed`、Aggregate/Final affected `13 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。准备阶段的 typed envelope 作为声明式 invariant 直接使用，没有新增缺失时 fallback/failed 分支，也未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-03/M8.4z 新增 Capability-owned `CrossProviderRuntime/CrossProviderComposition`：复用 account-scoped `RuntimeServicesView`、`ProviderAgentSessionFactory/AgentExecutionService`、按 owner `TaskEnvelope` scope 编译的 Artifact access、原 `RunToolResultIndex` 与声明 `submit_result` Tool，驱动 Cross initial owner Agent。真实 Tool Characterization 证明两个不同 Host Action ID 只用于 request trace，terminal 使用 envelope sender/task 和当前空 attempt，结果只从既有 `AgentResult.payload` 解码，同一 Conversation 连续两次调用仅创建一个 Provider session；独立导入不加载 `manyselves.core.reporting`。为移除测试掩盖，既有 direct bridge 与 full-tail Host fixtures 也改为 wrapper/envelope terminal，删除裸 submission fallback。Cross/provider + Tail affected `12 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。Cross findings 后的 revision/local-review/recheck/Main-exception 与 durable recovery 仍待后续切片。
- FA-03/M8.4aa 新增 Capability-owned `FinalProviderRuntime/FinalProviderComposition`：使用 account-scoped `RuntimeServicesView`、中立 Provider session/execution、按 Final `TaskEnvelope` scope 编译的 Artifact access、原 `RunToolResultIndex` 与声明 `submit_result` Tool，真实完成 Final initial Auditor finding。Characterization First 先因 composition 模块不存在取得 RED；GREEN 后真实 Tool 写出 `AgentResult` wrapper，typed payload 被 bridge 接收，同一 `final-chapter-<id>` Conversation 连续两轮仅创建一个 Provider session，独立导入不加载 `manyselves.core.reporting`。bridge 只增加已有 injected TaskCorrelation attempt 的 terminal 透传，不生成 attempt/hash/CAS/lease。Final Provider focused `2 passed`、Aggregate/Final affected `13 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；Final recheck 共用该 composition，但 Chief revision、durable recovery 与 Tail/Binding 注入仍待后续切片。
- FA-03/M8.4ab 新增 Capability-owned `FinalChiefProviderRuntime/FinalChiefProviderComposition`：使用 account-scoped `RuntimeServicesView`、中立 Provider session/execution、按 revision `TaskEnvelope` scope 编译的 Artifact access、原 `RunToolResultIndex` 与声明 `write_result_part/list_result_parts/submit_result` Tools，真实完成 Final Chief 章节修订。bridge 终端只接受 envelope sender/task 和已有 injected TaskCorrelation attempt，结果只从既有 `AgentResult.payload` 解码；同一 `chief-chapter-<id>` Conversation 连续两轮复用一个 Provider session。Characterization 真实调用三项 Tool 并核验持久化 wrapper identity/payload，focused `1 passed`；旧 Aggregate Host fixture 在同步 wrapper/envelope terminal 后全文件 `13 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口；Final Chief durable recovery 与 Tail/Binding 注入仍待后续切片。
- FA-03/M8.4ac 新增 Capability-owned `AggregateProviderRuntime/AggregateProviderComposition`：把文件任务的 `AggregateEditorInput` 机械投影成 Capability `TaskEnvelope`，复用 Preparation 已写入的 `context/aggregate-editor-input.json`、account-scoped Provider、同一 Bus、`ProviderAgentSessionFactory/AgentExecutionService`、按 envelope scope 的 Artifact access、原 `RunToolResultIndex` 与声明的 collaboration Tools。真实 `SubmitResultTool` 写出 `AgentResult` wrapper，bridge 固定只解码 `payload`，Host Action ID 不再伪装成 terminal task；同一 `aggregate-existing` Conversation 连续两轮只创建一个 Provider session。Characterization First 的真实 Tool wire/Provider ownership focused `2 passed`，同步两个旧裸 JSON fixture 后 Aggregate affected `13 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。无 TaskCorrelation 时保留既有 Tool 的空 terminal attempt，有注入时透传既有完整 correlation；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。生产 Binding 与 durable recovery 仍待后续切片。
- FA-03/M8.4ad 将通用 Module Revision preparation/apply/accept 物理收敛为 Capability 唯一实现：`runtime/module_revision_tools.py` 统一处理 module finding、Cross finding、用户 requested change 与结构验证触发，保留原 `ModuleRevisionInput`、完整 `TaskEnvelope`、用户补充约束、artifact delivery、revision diff、写入路径和 barrier；Core `review_lifecycle.py` 的 `prepare_module_revision`、`accept_module_revision`、`_apply_module_patch` 只剩参数投影与 `ReviewLifecycleError` 兼容边界。Characterization First 先因 Capability 通用入口不存在取得 RED，GREEN 后同时覆盖四种 target 来源与既有 barrier `subject_sha256`；Capability/Core revision-preflight/旧声明 lane focused `10 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片的 SHA 只是原 module barrier 字段机械迁移，没有新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口；Cross owner finding 生命周期仍在下一真实缺口 `prepare_revision` 处保持 RED。
- FA-03/M8.4ae 新增 Capability-owned `runtime/user_supplements.py`，把 Author、Auditor 与 Revision 的 supersedes/stage/scope/target 投影及原 Provider-visible 文案收敛为一个纯 typed helper；它同时消费活的 `ReportRequest` 和 checkpoint JSON 重建后的 Mapping。Module Revision 不再向 Core 索取 supplement callback，Core 薄适配只投影当前请求的 typed supplements。Characterization First 先证明 Mapping request 在旧 revision 调用中被静默丢失，GREEN 后 supplement 进入同一 `TaskEnvelope.constraints`；helper/revision focused `4 passed`、Author/Review/Revision affected `5 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。本片只修复序列化形态投影，没有新增 Gate、validator、Hash/CAS、锁/lease、重试、依赖、Action Kind 或公共 HTTP 接口；Cross revision 下一步可显式携带同一 typed supplements，不再依赖 Core 隐藏 state。
- FA-03/M8.4af 将 Cross owner 首轮 Author revision 接入 Capability `CrossOwnerRuntime`：initial typed state 显式携带当前 Run 的 `UserSupplement` 与既有 module review completion ref，revision 复用通用 `prepare_module_revision/accept_module_revision`，首轮按既有 r0 finding→r1 revision 语义生成完整 `TaskEnvelope`，并在 JSON checkpoint 重建后保留相同输入。same-run candidate 加载规则迁到 `module_revision_tools.py` 成为唯一实现，Core 旧 helper 只剩参数投影；已有候选按原路径、revision、finding response/target 规则复用，不重放 Author。Characterization 覆盖 fresh/restore/reuse、typed supplement、barrier、现有结果优先与 round 语义；Capability/Core/Cross affected focused 选择累计 `7 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。`subject_sha256` 仅是既有 module barrier 写出字段的机械保留，没有新增校验或恢复决策；未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。完整 finding 路径当前继续在 original Auditor local regression 的 typed lifecycle 缺口推进。
- FA-03/M8.4ag 将 Cross owner 修订后的 original Auditor local-regression 输入、准备和接受收敛到 Capability：`cross_local_regression.py` 显式消费既有 module review completion，保留 baseline/trigger diff、Cross finding/作者 response 与原 Auditor session；`prepare_module_local_regression_review` 生成 typed `ModuleReviewInput/TaskEnvelope`，`accept_module_initial_review` 成为 initial/local 共用的唯一持久化实现，Core 旧 local-context helper 只剩参数投影与异常翻译。同时修正 Public Host scripted fixture，使其使用生产的 `AgentResult.payload` 与 `TaskEnvelope` terminal identity，不再用裸 submission/Action ID 掩盖边界。Characterization First 后 local/runtime + Public Host + 旧 Core 对照选择 `7 passed`，Ruff、compileall 与 `git diff --check` 通过；`statement-<sha256[:12]>` 仅机械保留既有 Provider-visible Statement ref 命名，不参与恢复/CAS/Gate 决策。新的完整 finding 路径精确推进到 `prepare_recheck` RED；local preflight correction 和 local Auditor 新 finding 的再修订路由仍显式保留为下两个待收敛边界，未用 fallback 或无条件 goto 声称完成。
- FA-03/M8.4ah 将 Cross owner 首轮 finding 的 revision→original Auditor local regression→同 reviewer recheck→round advance→completion/promotion→aggregate reduction，以及 Author/Reviewer Main exception，接入 Capability-owned typed runtime；Cross 文件 Workflow 现在经真实 Definition/Registry、Compiler 与 Generic Host 完成一条 finding→resolved round，Reviewer Conversation/Session 保持复用。Cross specialization 已由 Capability `cross_owner_definitions.py` 物理拥有，独立导入不加载 `manyselves.core.reporting`；旧 Core 副本暂只服务尚未迁出的旧调用链。Provider loop 增加业务中立的同 session reconfigure，只把调用方当前 Provider、ToolRegistry、prompt、TaskEnvelope usage scope 和既有 observer 重新绑定到已恢复 loop；Module/Cross/Chief/Final/Template/Aggregate compositions 已统一使用，Cross Characterization 证明第二轮 ToolRegistry 生效且 loop 只创建一次。Cross reducer 现接受已闭合 finding pipeline 并写出既有 aggregate refs/状态；既有 ArtifactRef SHA 与 semantic key 只作为原产物协议机械保留，未新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。Characterization First 的 reducer RED 为旧实现拒绝闭合 finding；GREEN 后 Cross/Runtime 合并 focused `30 passed`，affected Provider `9 passed`、Module Provider `2 passed`、Public definition/Host `14 passed`、entrypoint contract `2 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。该证据使用 scripted Provider，不是最终真实 Provider/项目/浏览器验收；local Auditor preflight correction 与 local Auditor 产生新 finding 后的 Module revision/recheck 回环仍是下一显式缺口。
- FA-03/M8.4ai 将 Module initial-review 与 recheck 的机器预检 Author correction 接入 Capability-owned `module_preflight_revision.py`：文件 Workflow 现在通过已有 `prepare/accept-current-module-preflight-revision` Tool ID 生成 typed `ModuleRevisionPreparation/TaskEnvelope`、接受修订、恢复原 review progress，并分别回到 `authored` 或同一 `recheck_pending` 轮次；`CapabilityModuleRuntime` 与 Public Runtime 的两个真实端口不再缺失。`module_recheck_tools.py` 不再在预检失败时直接抛错，而返回 `preflight_revision_pending`；重试后保持原 review round。Legacy 已有 typed validation subject identity，以及相同失败签名 2 次或总计 3 次停止的 No-progress 规则，机械迁入 Capability `preflight_progress`，没有新增阈值、判断种类、Hash/CAS、锁/lease、Gate、validator、重试框架、依赖、Action Kind 或公共接口；`accept_module_revision` 原 barrier 中的 `subject_sha256` 仍只按既有产物协议写出。Characterization First 先以新增 runtime ports 得到 `1 failed`，GREEN 后 Module/Public affected `31 passed`、Legacy preflight 对照 `4 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。Cross local workflow 复用该端口的 typed state/YAML 路由仍是下一切片。
- FA-03/M8.4aj 新增 Capability-owned `distribution-cross-owner-local-module-review-lane` 文件子工作流并按五个 owner 静态 specialization：Cross revision 后先投影到 typed `DeclarativeModuleRuntimeLaneContext`，机器预检失败走原 Author correction，自然循环返回 local preparation；原 Auditor 初审产生 finding 时走同一原作者 revision、同一 Auditor recheck，复审预检也复用相同 typed correction，只有 local completion 后父 Cross workflow 才进入原 Cross reviewer recheck。`module_recheck_tools.py` 的 review root、lifecycle、task/finding id 与 progress ref 不再硬编码 initial，而从现有 preparation 投影；Core 旧 specialization 入口缩成 Capability 唯一实现的薄调用。没有用新增 `max_iterations` 绕过编译器，也未新增 Hash/CAS、锁/lease、Gate、validator、停止阈值、依赖、Action Kind 或公共接口；既有 module barrier SHA 和预检 No-progress 规则仅由原 Capability primitive 机械复用。Characterization First 的结构 RED 为缺少 Subworkflow，GREEN 后 local boundary `3 passed`，Generic Host 的 no-finding 与 local-finding/revision/recheck 两场景 `2 passed`，Module/Cross/Public affected `40 passed`，Definition/Legacy 失配精确修复选择 `5 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量；这些仍是 scripted Agent 证据，不是最终真实 Provider 验收。
- FA-04/M8.4ak 将五个公开 Reporting entrypoint 的生产 `DistributionReportingRuntimeBinding` 从 `ReportingFacade.start_declarative/resume_*` 完全切到 Capability-owned Generic Host runtime map：full/module 使用 `PublicReportingWorkflowRuntime + ModuleProviderComposition`，aggregate 使用 `PublicAggregateExistingWorkflowRuntime + Aggregate/Final/FinalChief Provider compositions`，template 使用 `TemplateDistillationWorkflowRuntime + Template Provider`，render 保持原 Generic Runtime。真实 Web 路径现在从 account-scoped `RuntimeHost` 投影 `RuntimeServicesView`，Capability 自行复用已有 `RunInputSnapshotStore`、`ContentAddressedStore` content view 与 photo projection，没有新增 Hash/CAS/锁/判断门禁。`WorkflowProjectionFacade/RuntimeBindingCatalog` 挂入 `TenantRuntime` 和单账户 lifespan，HTTP 请求复用同一个 AgentExecution/Conversation session owner；项目切换时关闭旧 Capability sessions 后按新 workspace 重建。Binding 源码架构 strict-xfail 已转为常规通过测试；Characterization First 的 Legacy call RED 与请求级 catalog reuse RED 均转绿，Application/Web/项目切换/架构 affected `44 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量。本片不宣称全部迁移完成或真实 Provider 已验证。
- FA-04/M8.4al 将 full-report 的 Cross owner、Chief chapter、Final Auditor 与 Final Chief revision Provider compositions，以及 Capability-owned Reporting tail workflow specializer/tools，注入同一个 `PublicReportingWorkflowRuntime`；`chief-editor` 仍由 Capability 按声明式 Task output contract 路由普通 Chief 与 Final Chief typed bridge，Kernel/Compiler/Host 不认识 Reporting 角色或阶段。full-report 真实编译计划现包含五个 Cross pipeline、local module review lane、Chief/Final/Delivery tools 及全部 typed Agent adapters；没有新增 Hash/CAS、锁/lease、Gate、validator、停止阈值、依赖、Action Kind 或公共接口。Characterization First 先以缺少 `distribution-cross-owner-2.1-pipeline` 得到 RED，补齐后 runtime/tail/provider focused `16 passed`，Public/Module/Cross/Aggregate affected `46 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量。该证据证明生产 binding 可编译完整文件定义并绑定 Generic Host，但仍是 scripted/offline 证据；恢复/持久化行为等价与最终真实 Provider/项目/浏览器验收继续后置。
- FA-04/M8.4am 在切换后的真实 `DistributionReportingRuntimeBinding` 上恢复 `render-existing` Characterization：测试先更新为 account-scoped `RuntimeServicesView(active_provider=None)`，随后暴露 `public_render_existing_input` 将项目相对路径保留为 `str`、且 Public typed model 未投影为内部 `RenderExistingRequest` 的两个 Definition/Capability 边界失配。修复仅把公开 Schema 模型字段恢复为 `Path`，并在 Render Capability runtime 进入 Generic Host 前做 Public→Internal typed projection；Kernel/Compiler/Host 未新增判断或业务类型。focused `2 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量；同时证明无 Provider 的 deterministic render 路径仍可独立完成。
- FA-03/M8.4an 新增 Capability-owned `agent_recovery_turn.py`，只把 AgentLoop 既有 natural-language、max-token 与 tool-slice terminal marker 投影为 Kernel recovery vocabulary，并把 Capability prompt/typed decoder 交给已有 `AgentExecutionService.execute_with_recovery + AgentRecoveryDriver`；Module Author `invoke_with_recovery` 现在在同一 Conversation/Session 内执行 submission correction、max-token continuation 和 tool-slice continuation，普通 `invoke` 仍保持单轮。Characterization First 的三种旧行为均先返回 `incomplete`，GREEN 后三项均完成第二个 typed turn；Module focused `15 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未新增 Hash/CAS、锁/lease、Gate、No-progress 算法、attempt/limit、依赖或公共接口；Schema-invalid callback、durable No-progress、Agent completed-result persistence reuse、Reviewer/Tail/Aggregate 同类恢复仍是后续显式缺口。
- FA-03/M8.4ao 将 account-scoped `DistributionReportingRuntimeBinding` 改为只创建一个 `AgentExecutionService`，并注入 Module、Cross、Chief、Final、FinalChief、Aggregate、Template 全部 Provider compositions；同一账户/工作区内不再因 Capability role composition 各自持有 session registry 而割裂 Conversation lifecycle，workflow id 与 conversation key 仍由现有 Runtime 服务隔离。Characterization First 先因 binding 没有单一 execution owner 得到 RED，GREEN 后断言全部 Provider 引用同一实例，binding focused `4 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未新增 session 合并规则、Hash/CAS、锁/lease、Gate、依赖或公共接口；新进程 AgentLoop history restore 与 completed-result persistence 仍需后续既有端口接线。
- FA-03/M8.4ap 将 Module Author Provider 边界接到既有 `load_completed_agent_result`：只有调用方提供完整 `TaskCorrelation` 时，bridge 才在创建 Agent session 前用 `AgentExecutionService.recover_completed_result` 解释 `COMPLETED_TOOL_RESULT`，并把已验证 `AgentResult.payload` 解码为原 typed Module result；命中时 Provider/session 创建均为 `0`。该切片不从 run/task/path 猜造 identity，也未新增或修改原 TaskAttemptStore 的 Hash/CAS、lease、attempt 或校验算法；默认 Web composition 尚未生成完整 correlation，因此本片只证明真实 ModuleProvider 可消费该既有持久化端口，不宣称生产默认已完成 same-process/new-process recovery。Characterization First 的 Provider 调用 RED 转绿后，Module focused `16 passed`，Legacy/Template completed-result 对照 `2 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。
- FA-03/M8.4aq 将 Cross owner initial/revision/local-review/recheck/Main-exception 共用的 typed Agent bridge 接入已有 `execute_reporting_recovery`：自然语言无结构化提交、Max Token 与 Tool Slice marker 现在均由 `AgentExecutionService.execute_with_recovery + AgentRecoveryDriver` 驱动，并在原 Conversation/Session 发送 Capability-owned correction/continuation prompt；普通 `invoke` 仍保持单轮。Characterization First 的三种 marker 在旧 initial-only 实现均返回 `incomplete`，GREEN 后同一 Provider loop 各完成第二个 typed turn；Cross/Tail affected `13 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未新增 Hash/CAS、锁/lease、Gate、No-progress 算法、attempt/limit、依赖或公共接口；Schema-invalid callback、durable No-progress 与 Cross completed-result persistence reuse 仍是后续显式缺口。
- FA-03/M8.4ar 将 Module Reviewer initial/recheck 共用的 typed bridge 接入同一个 Capability recovery adapter：自然语言无结构化提交、Max Token 与 Tool Slice marker 都由已有 `AgentExecutionService.execute_with_recovery + AgentRecoveryDriver` 在原 Conversation/Session 继续，Reviewer 只生成领域 prompt 并解码既有 `AgentResult.payload`。三项 Characterization 在旧 initial-only 实现先返回 `incomplete`，GREEN 后均完成第二个 typed turn；Reviewer focused `3 passed`、Public Runtime affected `19 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未新增 Hash/CAS、锁/lease、Gate、No-progress、attempt/limit、依赖或公共接口；Schema-invalid callback、durable No-progress 与 Reviewer completed-result persistence reuse 仍待接线。
- FA-03/M8.4as 将 Chief chapter typed Agent bridge 接入同一现有 Capability recovery adapter：自然语言无结构化提交、Max Token 与 Tool Slice marker 由 `AgentExecutionService.execute_with_recovery + AgentRecoveryDriver` 在原 Conversation/Session 继续，Chief 仅负责章节领域 prompt 与 `AgentResult.payload` typed decoder。三项 Characterization 在旧 initial-only 实现先返回 `incomplete`，GREEN 后均完成第二个 typed turn；Chief recovery focused `3 passed`、Chief Runtime/Provider affected `5 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量。未新增 Hash/CAS、锁/lease、Gate、No-progress、attempt/limit、依赖或公共接口；Schema-invalid callback、durable No-progress 与 Chief completed-result persistence reuse 仍待接线。
- FA-03/M8.4at 将既有 `ReportingAgentRunner._task_correlation` 的不可变输入、TaskEnvelope、subject 与 execution-profile identity 投影机械抽到 Capability-owned `completed_result_recovery.build_task_correlation`，旧 Runner 改为一行薄调用，确保 TaskAttemptStore correlation 只有一个实现。这里保留的 SHA-256、identity lease 与 terminal 校验均为原 completed-result 持久化协议，没有新增算法、CAS、锁、Gate 或校验条件；新 Generic Host Provider composition 仍不得从 Run ID 猜 correlation，也没有为通过测试反向导入 Legacy `ProviderRouter`。旧 Runner completed reuse/stable session 与 Capability loader focused `3 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。默认 Web 新路径仍需先把 Definition-owned Agent limits/Provider execution profile 抽成中立 typed composition，之后才能生成完全相同的 correlation。
- FA-03/M8.4au 修复已注入 `TaskCorrelation` 的 Module Author Provider terminal 对齐：bridge 现在把 correlation 现有 `task_attempt_id` 同时传给初始 `AgentTurnRequest` 与 `AgentResultMessage` terminal subscription，真实 `SubmitResultTool` 写出的 attempt 不再因 bridge 仍期待空串而超时；无 correlation 的现有路径继续使用原 Host task ID/空 terminal attempt。Characterization 将真实 Module Provider 的 scripted terminal 改为非空 attempt，旧 bridge 会等待错误 terminal，GREEN 后同一 Provider session 两轮完成；该测试与 persisted completed-result reuse 合计 `2 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量。未生成 correlation、Hash/CAS、lease 或新判断门禁，只消费调用方已拥有的 typed identity；默认 Web correlation 仍未宣称完成。
- FA-04/M8.4av 新增 Runtime-owned detached Run lifecycle：每个文件入口显式提供 `run_id_for`，`StartAwareFileWorkflowStateStore` 在 Generic Host 第一次真实持久化后发出进程内信号，`DetachedRunTaskOwner` 让 FastAPI POST 在可查询状态存在后返回、同时继续执行原 Host coroutine；Host 仍唯一拥有 RUNNING/WAITING/FAILED/COMPLETED 状态，Binding 不猜 Run ID、不反射私有 store，也不取消已受理任务。项目切换复用既有 `RUNTIME_BUSY` 生命周期约束并纳入 active Generic Run，避免把持久化 RUNNING 留在被关闭的 workspace；关闭时等待 Host 写出终态。过期的 Web Characterization 已从已删除公开入口 `distribution-reporting`/Legacy controller 改为真实 `full-report` 文件入口，证明 202 后同 Run 可查询且 Legacy controller 调用为 `0`。Runtime/Binding/Projection/项目/五个入口 affected 两组分别 `32 passed`、`46 passed`，真实 HTTP 与 RuntimeServices focused `6 passed`；定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。RuntimeServices 对没有 ProviderManager/global knowledge 的现有 degraded/test Host 只投影原有 `None`，未增加执行 Gate。未新增 Hash/CAS、锁/lease、重试、validator、Action Kind、依赖或公共接口；`provide_input` 仍同步等待 Host，是下一自动切片。
- FA-04/M8.4aw 将既有 Capability `provide_input` acceptance 端口接入同一 detached lifecycle：Distribution 长任务在 Generic Host 写出恢复后的下一状态后返回 202，后续 Agent/Tool/Interaction 继续在同一 Run、同一持久状态和同一 Host coroutine 中执行；Parameter Adjustment 等短确定性 Capability 仍可在原端口内同步完成。没有新增 `provide_input_detached` 长期接口、重复提交门、重试或新状态机。Characterization First 先得到 Facade 阻塞超时与 Distribution Binding 缺少异步恢复边界 `2 failed`，收敛后真实 Binding 证明 WAITING→RUNNING 即受理、后台最终 COMPLETED。过期的嵌套 HTTP 测试也移除未声明临时 Workflow→Legacy controller 的旁路，改为测试 Capability Binding + 真实 WorkflowProjectionFacade + HTTP route + Generic Host，继续证明外→内 path、同 Run 递归恢复、已完成右兄弟不重放与 Join 结果。Projection/Binding/Parameter/Host/HTTP affected `48 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未新增 Hash/CAS、锁/lease、Gate、validator、Action Kind、依赖或公共接口。
- FA-03/M8.4ax 将 Module Author/Reviewer 的 Capability-owned `SubmitResultTool` schema-invalid callback 接到 Kernel 已传入的同一 `RecoveryPolicyDefinition` 与同一个 `AgentRecoveryDriver`：Tool 内第一次无效平面提交仍返回原 `correction_required`，同一 Provider Conversation/Session 随后提交 typed result；Controller 现在真实观察 `invalid_structured_output → correct`，而自然语言、Max Token 与 Tool Slice recovery 共享同一内存 attempt state。事件通过当前 Tool 调用闭包绑定，不扩展 `ToolResult` 消息，也不猜 workflow/run/task/session。Characterization First 在纠正成功但 Controller observation 为空时取得 RED，GREEN 后真实 Module Provider/SubmitResultTool 完成无效→有效提交。该测试还发现 `publish_research_note` 虽已有 Capability YAML/Python Tool、但 Provider `available` 漏装配；现已直接实例化既有 `PublishResearchNoteTool`，不再靠测试 dummy。Module/marker 选择 `5 passed`，Reviewer 与 Legacy schema 对照 `4 passed`；定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未新增 Hash/CAS、锁/lease、Gate、validator、attempt limit、依赖、消息字段或公共接口；Cross/Chief/Final 同类 Tool callback 继续按相同机械方式迁移。
- FA-03/M8.4ay 将 Cross Provider 的 Capability-owned schema-invalid Tool callback 与已有 Cross correction/continuation loop 接到同一个 `AgentRecoveryDriver`：Provider 仅在 Kernel 传入声明式 policy 时创建 driver，在调用方未注入既有 callback 时把该 driver 绑定到 `SubmitResultTool`，同时把相同实例交给 `CrossOwnerAgentInvoker` 的 `execute_reporting_recovery`；普通单轮 invoke 与显式 callback 注入保持原样。短 Characterization First 先因 `_bridge` 不接受 policy 得到 RED，GREEN 后直接证明 Tool 的 `invalid_structured_output → correct` 与 Cross Agent recovery 共用同一 attempt state；Cross Provider 全文件及三种同会话 marker affected 共 `8 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未修改 Kernel/Compiler/Host，未新增 Hash/CAS、锁/lease、Gate、validator、attempt limit、依赖、消息字段或公共接口；Chief/Final 同类 Tool callback 继续自动迁移。
- FA-03/M8.4az 将 Chief Provider 的 Capability-owned schema-invalid Tool callback 与已有 Chief correction/continuation loop 接到同一个 `AgentRecoveryDriver`：声明式 policy 只在 `invoke_with_recovery` 进入 Provider 组装，未覆盖调用方已有 callback；同一 driver 同时绑定现有 collaboration Tools 与 `ChiefChapterAgentInvoker.execute_reporting_recovery`，普通单轮执行、Chief 领域 prompt、Tool 列表及文件定义均不变。短 Characterization First 先因 `_bridge` 不接受 policy 得到 RED，GREEN 后证明 Tool 的 `invalid_structured_output → correct` 更新的就是 Agent loop 使用的 attempt state；Chief Provider、三种 recovery marker 与 Chief Runtime affected 共 `9 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未修改 Kernel/Compiler/Host，未新增 Hash/CAS、锁/lease、Gate、validator、attempt limit、依赖、消息字段或公共接口；Final/FinalChief/Aggregate/Template 继续按各自真实 bridge 边界勘察后迁移。
- FA-03/M8.4ba 将 Final Auditor initial/recheck 共用 bridge 从丢弃 recovery policy 的单轮端口推进到真实声明式恢复：`execute_reporting_recovery` 在原 Conversation/Session 继续自然语言纠正、Max Token 与 Tool Slice 既有事件，Final Capability 只生成领域恢复 prompt 并复用既有 typed decoder/terminal correlation；Provider 同时把同一 `AgentRecoveryDriver` 绑定到 collaboration Tool schema-invalid callback，未覆盖调用方已有 callback。Characterization First 先得到 natural-language 后 `incomplete` 的 RED，再得到 Provider `_bridge` 不接受 policy 的 RED；GREEN 后证明第二轮 `submission_correction` 同会话完成 typed result，且 Tool 的 `invalid_structured_output → correct` 更新同一 attempt state。Final Provider 与 Aggregate affected 共 `17 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未修改 Kernel/Compiler/Host，未新增 Hash/CAS、锁/lease、Gate、validator、attempt limit、依赖、消息字段或公共接口；FinalChief/Aggregate/Template 继续按真实 bridge 边界迁移。
- FA-03/M8.4bb 将 Final Chief revision bridge 从丢弃 recovery policy 的单轮端口接入现有 `execute_reporting_recovery`，Capability-owned prompt 明确在原会话复用已完成分析/Tool 结果并提交 typed revision；Provider 同时把相同 `AgentRecoveryDriver` 绑定到 collaboration Tool schema-invalid callback，且保留调用方已有 callback。为减少重复脚本，短 Characterization First 先因 Provider `_bridge` 不接受 policy 得到 RED，GREEN 后捕获真实 bridge 对通用 recovery adapter 的调用，证明 Tool 的 `invalid_structured_output → correct` attempt state 与 Agent recovery driver 为同一实例；同会话连续纠正行为由同一 adapter 及前一 Final 垂直切片覆盖。Final Chief Provider 与 Aggregate affected 共 `15 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未修改 Kernel/Compiler/Host，未新增 Hash/CAS、锁/lease、Gate、validator、attempt limit、依赖、消息字段或公共接口；Aggregate/Template 继续迁移。
- FA-03/M8.4bc 将 Aggregate Editor bridge 从丢弃 recovery policy 的单轮端口接入现有 `execute_reporting_recovery`，Aggregate Capability 继续只拥有 `AggregateEditorInput` prompt 与 `EditedReportSubmission` decoder；Provider 把同一 `AgentRecoveryDriver` 绑定到 collaboration Tool schema-invalid callback 与 Agent recovery，且保留调用方已有 callback。短 Characterization First 先因 Provider `_bridge` 不接受 policy 得到 RED，GREEN 后捕获真实 adapter 调用并证明 Tool 的 `invalid_structured_output → correct` 与 Agent recovery 共用同一 attempt state。Aggregate Provider 与完整 Aggregate file workflow affected 共 `16 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未修改 Kernel/Compiler/Host，未新增 Hash/CAS、锁/lease、Gate、validator、attempt limit、依赖、消息字段或公共接口；Template Tool schema event 与 durable No-progress 仍待后续迁移。
- FA-03/M8.4bd 将 Template Distillation Provider 的 Capability-owned schema-invalid Tool callback 与既有 completed-result、自然语言纠正、Max Token、Tool Slice 恢复接到同一个 `AgentRecoveryDriver`：Provider 在声明式 recovery policy 边界创建 driver，把它同时传给 `build_template_distillation_provider_tools` 和 `TemplateDistillationAgentBridge`；bridge 不再为 pre-session reuse 与同会话 continuation 分别创建私有 driver。短 Characterization First 先因 Provider `_bridge` 不接受 policy 得到 RED，GREEN 后证明 Tool 的 `invalid_structured_output → correct` 与 bridge recovery 共用同一 attempt state；Template focused 全文件 `15 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。未修改 Kernel/Compiler/Host，未新增 Hash/CAS、锁/lease、Gate、validator、attempt limit、依赖、消息字段或公共接口；durable No-progress 仍待下一切片机械复用既有语义。
- FA-03/M8.4be 抽取 Reporting durable No-progress 的第一层中立端口与 Capability 唯一快照实现：`execute_reporting_recovery` 在 Max Token/Tool Slice marker 上可调用注入的 progress observer，并把 `progressed/no_progress` 作为既有 `AgentRecoveryProgress` 交回通用 Runtime/Controller；`continuation_progress.py` 机械承接旧 Runner 对 durable files、result parts 与去重 semantic conversation events 的既有摘要算法和同一路径持久基线，旧 Runner 两个 snapshot helper 改为薄委托，避免新增第二套 hash 逻辑。Characterization First 分别以未知 `progress_observer` 参数和缺少 Capability 模块取得 RED；GREEN 后证明 observer 重建后相同事件为 no-progress、新 durable artifact 恢复为 progressed。Capability/Runtime/旧 Runner productive/no-progress focused `6 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。本片仅物理迁移原 SHA-256 进度摘要，不增加摘要字段、CAS、锁、Gate、validator、停止阈值、依赖、消息字段或公共接口；Provider bridges 的 observer 注入与真实同会话停止是下一切片。
- FA-03/M8.4bf 将 Template Provider 的真实 `TaskEnvelope` 绑定到 Capability durable progress observer，并把同一个 observer 注入既有 Template recovery bridge；Max Token/Tool Slice marker 现在先形成 `AgentRecoveryProgress`，再由相同声明式 policy/driver 决定继续或 No-progress 停止，Conversation/Session 和 completed-result/schema recovery 组合均不变。Characterization First 在 Provider 已共享 Tool/Agent driver 但 bridge 不暴露 progress observer 时取得 RED；GREEN 后 Template focused 全文件 `15 passed`，progress adapter/observer focused `2 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。摘要算法只调用 M8.4be 的唯一既有实现，未新增 Hash/CAS、锁/lease、Gate、validator、停止阈值、依赖、消息字段或公共接口；Module/Cross/Chief/Final/Aggregate Provider 注入继续机械扩展。
- FA-03/M8.4bg 将同一个 Capability durable progress observer 机械注入 Module Author/Reviewer、Cross、Chief、Final Auditor、Final Chief 与 Aggregate Provider bridges：每个 observer 只绑定该准备阶段已有的真实 `TaskEnvelope`，每个 bridge 仅把它透传给已验证的 `execute_reporting_recovery`；Tool schema callback、Agent recovery 与 progress recovery 继续共享原 policy/driver，未在 Kernel/Compiler/Host 增加角色或状态。Template 的 RED 已确立该组合缺口，随后各 Provider 现有 schema/recovery Characterization 增加 observer 存在性证明；Module 三类 marker、Reviewer 三类 marker、Cross/Chief/Final/FinalChief/Aggregate 选择共 `13 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。全部摘要与持久化仍调用 M8.4be 唯一实现，未新增 Hash/CAS、锁/lease、Gate、validator、停止阈值、依赖、消息字段或公共接口；下一步转向新生产路径的完整 persisted completed-result correlation 接线与 Legacy 物理删除。
- FA-05/M9.1 移除 Legacy Reporting 的 FastAPI/Application 生产可达入口：`main.py` 不再挂载 `/api/v1/reporting/*` router，单账户与多租户 lifespan 不再构造、关闭或重绑 `ReportingFacade`；维护窗口与项目切换只观察现有 `WorkflowProjectionFacade.active`，五个 Reporting 文件入口继续由 `/api/v1/workflows` 的 Capability Binding/Generic Host 执行。Characterization First 新增架构断言并先捕获 `reporting_router/ReportingFacade` RED；GREEN 后架构、Application binding、Workflow HTTP projection 与项目切换 focused `24 passed`，定向 Ruff、compileall 与 `git diff --check` 通过，未跑全量。此处只是移除旧服务，不新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口；旧 route/facade 源文件、Core Runner/Tool 构造与旧前端 Reporting feature 尚待后续物理删除。
- FA-05/M9.2 物理删除已不可达的 `application/reporting_facade.py`、`webapi/routes/reporting.py` 与 `webapi/schemas/reporting.py`，并删除只验证旧 Facade 自身读取旧 workspace 的 release golden；通用 Workflow schemas/routes 与 Capability-owned output projection 保留。Characterization First 在 M9.1 架构测试追加三个文件必须不存在的断言并取得 RED；删除后架构 + Workflow HTTP focused `17 passed`，源码 import 扫描为 `0`，定向 Ruff 与 `git diff --check` 通过，未跑全量。没有加入 fallback、兼容 shim、Gate、Hash/CAS、锁/lease、validator、重试、依赖或新公共接口；旧 Core Runner/Reporting Tool 及未路由的前端 Reporting feature 继续删除。
- FA-06/M9.3 删除未路由但仍在 App 生命周期中维护的 React `features/reporting` 专属 Workspace/API/Store：SSE 的 `workflow/action/output/report/reporting` 事件现在按 payload 中现有 `run_id` 统一失效 `['runs', run_id]`，由通用 `RunWorkspace` 的 Run/Output/Cost/Event queries 投影，不再维护第二套报告快照。Characterization First 将旧“写 ReportingStore”测试改为“刷新 Generic Run”并先得到仅失效 `['reporting']` 的 RED；GREEN 后 App + Run focused `15 passed`。同时从当前 FastAPI 重新生成 OpenAPI JSON 与 TypeScript schema，确认 `/api/v1/reporting`、ReportingStore 和旧 feature 引用为 `0`；定向 ESLint、TypeScript、API contract check 与 `git diff --check` 通过，未跑全量。未新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口；下一步继续删除 Main loop 可达的 Legacy Reporting Tool/Core Runner。
- FA-05/M9.4 移除 Main Conversation 到 Legacy Reporting 的最后一条生产旁路：LoopManager 不再注册 run/resume/revise/cancel/status 五个 Reporting Tools，`core/tools/reporting_tool.py` 与其后台 controller 测试已物理删除，ToolOutcome 不再把这些名字特殊化为 terminal。更重要的是，通用 `AgentLoop` 已删除配电报告文本识别、直接自动恢复、取消许可、证据决策许可、报告文件读取拦截、失败说明重写与交付回合拦截等 Capability 业务门禁；Main prompt 改为 Capability-neutral，并明确文件 Workflow 由通用 Run 生命周期负责。Characterization First 新增源码/文件边界断言并先 RED，完成后 Manager/Prompt/ToolOutcome/架构 focused `39 passed, 1 xfailed`，AgentLoop 受影响流式、Tool、Max Token 与并行 pure-read 选择 `4 passed`；定向 Ruff（仅忽略该文件既有 N818 命名债务）、compileall 与 `git diff --check` 通过，未跑全量。通用 runtime/application/webapi 已无 `core.reporting` import，原 strict-xfail 现转为普通 GREEN。没有用 Feature Flag/白名单替代，没有新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口；headless、Product Skill Maintainer 与 Core Reporting Runner 本体仍待后续物理拆除。
- FA-05/M9.5 删除与当前 FastAPI/React Generic Workflow 产品面并列的旧 Report-only headless API/worker：移除 `manyselves-headless` console entry、`headless_service.py` 及 Core Reporting 的 headless/web/production/job runtime 四文件和专属测试，Packaging 只保留现有 Manyselves 启动入口。Characterization First 新增包装边界并先在 console script 处 RED，GREEN 后架构/branding `9 passed, 1 xfailed`，源码扫描确认相关生产类型为 `0`。同一切片还从 Main ToolRegistry 移除 Project/Product Skill Evolution 两个 Distribution-specific 工具，避免后者继续直接构造 `ReportingAgentRunner`；Manager/架构 focused `19 passed`。定向 Ruff 与 `git diff --check` 通过，未跑全量。Capability 自己的 Skill 定义/文件运行边界未删除；没有新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口。Core Runner 目前只剩旧 Core 内部/GUI/测试引用，继续按生产可达链拆除。
- FA-05/M9.6 解除通用 `LoopManager` 对 Distribution Reporting Agent 定义/Prompt 的隐式依赖：Main loop 不再调用 `load_packaged_agents()` 或把 `PromptAssembler.system_prompt(main-agent)` 拼入系统消息，只加载自身 Capability-neutral Markdown 与可选 Shared context；Reporting 角色定义继续只在 Capability 文件 Workflow 的 Provider composition 中消费。Characterization First 先在 `load_packaged_agents` 源码断言处 RED，GREEN 后 Manager/架构 focused `19 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量。没有复制 Prompt、增加 fallback、Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口。
- FA-05/M9.7 清空 Distribution Capability package initializer 对旧 Core Reporting 的反向兼容导入：`adapters/__init__.py` 现在只声明应用适配器包，运行时加载 `adapters.runtime` 不再急切导入旧 config、module cohort/lane 或 reporting tail。Characterization First 将 Core import 扫描扩展到整个 Capability root 并准确捕获唯一违规文件后 RED；旧 Runner 测试暂需的 Kernel Agent→旧 Runner model 投影已收回旧 `core.reporting.config` 内部，旧 Core 自身不再要求 Capability 提供兼容 adapter。架构与三项受影响定义选择 `5 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量；生产 import smoke 由 luna_worker 独立确认 Web、WorkflowProjection、Capability Binding、LoopManager 均未加载 `manyselves.core.reporting`。没有新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口；该旧 Core 内部投影会随 Runner 包物理删除。
- FA-05/M9.8 删除四个已无生产注册但仍可显式重入旧 Core Reporting 的残片：`core/context_manifest.py` star-import shim、`core/tools/skill_evolution_tools.py`（含直接构造旧 Runner 的 Product maintainer）以及两个旧 Reporting benchmark/storage preview 脚本。Characterization First 新增文件不存在边界并先在 context shim 处 RED；GREEN 后该边界与 Capability Core-import 扫描 `2 passed`，外部生产源码对 `manyselves.core.reporting` 引用为 `0`，定向 Ruff 与 `git diff --check` 通过，未跑全量。没有添加替代 shim、Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口；下一步清理 Qt 专属表面后物理删除自包含旧 Core 包和旧测试。
- FA-06/M9.9 清理 Qt Agent UI 中最后的 Reporting 专属角色与 Run 边界：启动时恢复全部已有 Agent Conversation，而不是只恢复 `main/report-workflow`；任意 Workflow/Agent 状态变化只更新自己的通用状态，不再用 `report-workflow` 的新 Run 信号清空其他 Agent 行、缓存或当前选择。通用 `ReportMessage` 仍作为任务/子智能体消息类型保留，没有把名字相似但业务中立的消息协议误删。Characterization First 新增桌面 UI 源码边界并先在 `report-workflow` 硬编码处 RED；GREEN 后架构、MainWindow switching 与 Sidebar focused `9 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量。没有新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口；下一步物理删除已无生产入口的旧 `core/reporting` 包与其专属测试。
- FA-05/M9.10 物理删除已无生产入口的 `manyselves/core/reporting` 全部 44 个 Python 源文件及 `tests/reporting` 89 个 Legacy 专属测试文件，并删除混合了大量旧 Runner 兼容比较的 `test_definition_package.py`；Capability 测试改为直接验证 `DeliveryTools`、Module revision、Reporting tail specializer、typed Agent bridges 与 Capability Tool，不再实例化或 monkeypatch 已不存在的 Runner。Characterization First 先以“Core Reporting Python 包必须为空”和“声明式 Runner 不得继承 Legacy”得到 `2 failed`；删除后架构 `12 passed`，关键 Definition/Compiler/Generic Host/Capability/Provider/Delivery 受影响选择 `103 passed`，物理 ownership 与 run-135 Tool contract 选择 `62 passed`，Capability 测试目录 `256 tests collected` 无收集错误。只清除了三个精确的可再生 `__pycache__` 目录以消除 namespace package 假象；没有删除用户数据，也未新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口，未跑全量。下一步清理旧 Web 测试/Release 清单与历史产品矩阵中的失效 Reporting 路径，再做最终自动架构审计。
- FA-05/M9.11 清理混合 Web 合同文件中已经随生产路由删除的 `_ReportingController`、`ReportingFacade` 生命周期支撑和 `/api/v1/reporting/*` 快照/启动/取消测试；保留 Conversation、Agent、Checkpoint、Python、Settings 以及 `/api/v1/runs` 通用 Workflow HTTP 测试，并将项目切换断言对齐当前 `WorkflowProjectionFacade.workspace`。Release 的 runtime-shutdown 证据改指现有 Application RuntimeHost 的 orderly-drain Characterization，不以删除的旧 Facade 测试维持假覆盖。Luna 只读审计先按函数边界区分 Legacy 专属与通用合同；修改后目标文件 `96 tests collected` 无收集错误，项目重绑、两个生产 Capability、项目隔离、嵌套 WAITING 同 Run 恢复和 release 证据 focused `14 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量。没有恢复兼容路由，也未新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口；下一步清理 SSE 测试和当前实施/发布文档的旧引用。
- FA-05/M9.12 将 SSE/Application lifespan 的通用资源所有权 Characterization 从已删除的 `ReportingFacade` 机械对齐到 `WorkflowProjectionFacade`：清理顺序现在验证 producers→Capability runtime→Python→Conversation→broker→bus，启动失败、清理失败、原始异常保留、secret-safe diagnostics、pending cleanup retry 和 single-stop Host 兼容语义均继续保留。测试 double 只补齐当前业务中立 `RuntimeServicesView` 已要求的 `get_loop/config_manager` 端口，Peer query/reply session 测试也移除误导性的 Reporting 命名。旧测试先得到 `ReportingFacade` AttributeError 与缺失 runtime view port 的 RED；GREEN 后相关 lifecycle/peer focused `14 passed`，定向 Ruff 与 `git diff --check` 通过，未跑全量。生产代码未改，没有新增 Gate、Hash/CAS、锁/lease、validator、重试、依赖或公共接口；下一步按只读审计更新当前实施/发布文档引用并执行最终自动架构审计。
- FA-07/M9.13a 对齐当前网页交接与发布矩阵：最终真实入口从已不存在的 `distribution-reporting` 更正为 Capability 声明的 `full-report`，表单不再要求由 Workflow ID 固定并从公共 Schema 隐藏的 `operation`；REPORT-001～010 的证据改为 Generic Workflow API/RunWorkspace，并新增一条 Playwright 浏览器 Characterization，实际从 `full-report` Schema 表单启动，经通用 `/runs` 投影 completed 状态、事件、DOCX Output 与 token/cost。Feature matrix 先因 Reporting 行标为 tested 却缺 browser evidence 得到 RED；浏览器首次安装运行时后又以严格 locator 歧义 RED，修正为 heading role 后 focused Playwright `1 passed`，matrix `3 passed`，定向 ESLint、TypeScript 与 `git diff --check` 通过。该浏览器测试使用受控 API fixture，不是最终真实 Provider 验收；独立架构审计同时发现 Module 8 个生命周期端口和 production completed-result correlation 尚未闭环，因此 Program 继续保持 in progress。未新增生产依赖、Gate、Hash/CAS、锁/lease、validator、重试或公共接口。
- FA-04/A6 让 `PublicReportingWorkflowRuntime.start` 在执行 full/module 文件 Workflow 前创建真实 Run 输入快照：直接复用既有 `RunInputSnapshotStore.freeze(run_id)` 固化 `Inputs/Knowledge/Templates`，再进入同 Run Generic Host；Preparation 收到的 loader 随后可读取同一个 manifest，不再依赖测试预先注入快照。Characterization First 在旧实现以 manifest 不存在得到 `2 failed`，GREEN 后 full/module 均证明 freeze 先于 execute、同 Run load 可恢复；主线程交叉选择 `3 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。审计时删除了用 `model_construct` 绕过 `ReportRequest` 校验的非法 source-ref case；render/aggregate 的显式 refs 仍由各自 runtime 负责。本片只调用既有 snapshot/CAS/lock 机制，未新增或修改 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-04/A7 新增 application-facing `PublicAggregateExistingWorkflowRuntime`：以 `aggregate-existing-{command_id.hex}` 分配 Run，验证公开 user Schema，把 `request + run-id` 注入文件 root，在 Generic Host 前用原 `RunInputSnapshotStore.freeze(extra_refs=...)` 固化五个显式 `source_module_refs` 或默认 `Outputs/Modules/2.1～2.5.md`，并复用现有 Aggregate/Final/Delivery Tool factory、subworkflow Plan、注入的 Agent invokers与通用 state/event store。该 runtime 还提供与其他文件入口一致的 async input 和 run/output/cost 投影；无 WAITING 时使用既有 `CapabilityRunInputError`。Characterization First 先因类不存在取得 RED；GREEN 后合法五模块输入已推进到第一个 `missing agent adapter` 真实边界，snapshot、root/subworkflow 持久状态和查询接口均可检查；Luna focused `3 passed`，主线程新入口选择 `1 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。Aggregate Provider composition 仍待接线，本片无 Legacy fallback，未复制或新增 Hash/CAS、锁/lease、Gate、validator、重试、依赖、Action Kind 或公共接口。
- FA-02 新增 `runtime/agent_execution.py`：服务直接管理 `(workflow_id, conversation_key)` session registry、AgentLoop 创建/恢复/启动/复用、单轮消息发布与 terminal 等待、turn completion 和 workflow close；该模块只依赖中立 `AgentSessionLoop`/`AgentMessageBus` 结构协议，不导入历史 `core` 具体类。Reporting 生产路径已改用 `start_or_restore`、`dispatch_turn`、`wait_until_turn_complete` 和 `close_workflow`，不再直接 `loop.start/stop`、`bus.wait_for` 或构造 `UserMessage`。
- AgentExecutionService Characterization 先因模块不存在取得 RED；Runtime execution/adapter/recovery `13 passed`，Reporting stable-session/persisted-recovery/auditor-isolation/max-token/tool-slice/no-progress/correction `8 passed`，声明式 recovery policy 选择 `5 passed`。定向 Ruff、compileall、5 项架构 strict xfail 和 `git diff --check` 通过。新服务未新增 Gate、Hash、CAS、锁或生产依赖；Reporting 原有 correlation/hash 仍原地保留且未复制。
- FA-02 恢复循环继续收敛：Runtime 用 `AgentRecoveryCompleted/Required/Stopped` 分型观察直接分派 RecoveryController 的动作，Capability 端口统一为 async 的 terminal 解释、领域消息构造、stop 持久化与 result reuse；移除了可冲突 boolean/optional 组合、sync/async 双态、`None` 拒绝语义和 action compatibility 判断门禁。Reporting 的真实 natural-language-without-submission 现在通过 `execute_with_recovery` 在原 session 执行 CORRECT/STOP/FAIL，Reporting 仅构造 correction Prompt、解码 typed result 和保留既有结果持久化。
- 上述恢复循环切片的 Runtime/Recovery/Reporting focused 选择 `22 passed`，定向 Ruff、compileall 和 `git diff --check` 通过。未新增 Hash、CAS、锁、Gate、attempt limit 或依赖；唯一异常分支是执行已声明 `FAIL` action 的必要语义。
- FA-02 continuation 生产切片把 max-token 与 tool-slice sentinel 转为 `AgentRecoveryRequired` 并交给 `AgentExecutionService.execute_with_recovery`；Runtime 现在执行 Controller 的 CONTINUE/STOP/FAIL 并通过同一个 `execution_session` dispatch 下一轮，Reporting 只保留领域 continuation Prompt、typed result 解码、已有进度快照与 continuation state 持久化。两项生产 spy Characterization 实现前 `2 failed`、实现后转绿；Agent/Runtime/Reporting focused `23 passed`，主工作区复核选择 `12 passed`，Ruff、compileall、5 项架构 strict xfail 和 `git diff --check` 通过。未复制或新增 Hash、CAS、lease、锁、Gate、attempt limit 或依赖；typed no-progress 与 completed-result reuse 仍明确为待迁移。
- FA-02 typed progress/no-progress 切片新增不可歧义的 `AgentRecoveryProgress(progressed|no_progress)`，Runtime 负责调用 `observe_progress`、形成 `NO_PROGRESS` directive 并执行 CONTINUE/STOP/FAIL；Reporting 删除私有 action 决策/兼容校验，只提供既有 durable/conversation snapshot、既有 no-progress 阈值、状态持久化和领域 stop result。生产观察 Characterization 实现前 `2 failed`、实现后转绿；Agent/Runtime/Reporting focused `25 passed`，主工作区复核 `13 passed`，Ruff、compileall、5 项架构 strict xfail 和 `git diff --check` 通过。Runtime 未复制 Reporting 的 hash snapshot 或阈值，未新增 Gate、Hash、CAS、lease、锁、attempt limit 或依赖；completed-result reuse 仍为下一项债务。
- FA-02 最终 completed-result 切片新增 pre-session `AgentExecutionService.recover_completed_result`，在 Reporting 已完成既有 correlation/terminal/identity 验证和 typed decode 后，由 Runtime 主调 `COMPLETED_TOOL_RESULT` 的默认/声明式 REUSE_RESULT、STOP、FAIL。生产 spy Characterization 实现前 `1 failed`，实现后 Provider 调用仍为 `0`、Runtime session registry 为空、原 session identity 和旧 typed result 原样复用。Runtime/Reporting affected `26 passed`，主工作区复核 `15 passed`，Ruff、compileall、5 项架构 strict xfail 和 `git diff --check` 通过；未复制或新增 Hash、CAS、lease、锁、Gate、attempt limit、action compatibility 校验或依赖。至此 FA-02 规范列出的生产 Agent/Conversation/Recovery 主调债务清完，测试专用 Legacy adapter 留到 FA-05 删除。
- FA-06/M9.14 将业务中立的 Workflow/Peer/Progress/Research/Blocked/AgentResult 消息从 `reporting.*` 产品事件名统一为 `workflow.*`，`ReportMessage` 继续使用通用 `report.status.changed`；React 查询失效逻辑同步移除 `reporting.` 特判。Characterization First 在旧映射上取得 `3 failed`，GREEN 后 Event Mapper/SSE focused `89 passed`、App Vitest `5 passed`，定向 ESLint、TypeScript 与 `git diff --check` 通过；未更改 Kernel、Compiler、Capability 行为或公共 HTTP 结构，未新增 Gate、Hash、CAS、锁、校验链或依赖。
- FA-05/M9.15 将仍在生产 `RuntimeFacade` 中使用、但行为仅为通用只读投影的 `LegacyRuntimeAdapter` 更名并物理迁移为 `RuntimeSnapshotAdapter`，旧模块、旧类名和兼容 shim 均删除。Characterization First 先因新模块不存在取得 `ModuleNotFoundError` RED，GREEN 后 Application facade focused `26 passed`，定向 Ruff、compileall、旧名称扫描与 `git diff --check` 通过；没有改变快照结构、锁/命令语义或运行路径，也未新增 Gate、Hash、CAS、锁、校验链或依赖。
- FA-05/M9.16 将 `runtime/tool_adapter.py` 中仅由旧测试消费的 `LegacyToolAdapterFactory` 和 `legacy:*` 解析路径物理删除；生产 `CapabilityToolAdapter` 改为继承中立 `ToolAdapter`，只共享声明元数据与现有 completed Tool Result lookup，Capability callable、Contract 校验和 result index 行为保持不变。Characterization First 先因中立基类不存在取得 `ImportError` RED，GREEN 后 Runtime/Parameter Adjustment/Reporting Tool affected focused `44 passed`，定向 Ruff、compileall、生产旧名称扫描与 `git diff --check` 通过；未新增 Gate、Hash、CAS、锁、校验链或依赖。
- FA-03/M9.13b 将真实 Binding 缺少的 8 个 Module lifecycle Tool 端口全部接入 `CapabilityModuleRuntime`：initial review/recheck 的 durable `review/revise/completed` 投影、Author/Reviewer Main exception、既有决定恢复、同 Run `request_user`、`return_to_author`、`stop_incomplete` 和 reviewer escalation 后的 recheck acceptance 均由 Capability typed state 驱动，Kernel/Compiler/Host 不认识这些 Reporting 语义。静态端口 Characterization 先证明 module/full 两个公开根均少 8 个绑定；实现后新增 7 个行为场景并复核 Public Host。Module focused `26 passed`、Public Host 精确路径 `1 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；状态 identity/completion 条件机械保留自既有 lifecycle，未新增 Hash/CAS、锁/lease、Gate、停止阈值、依赖、Action Kind 或公共接口。
- FA-04/M9.17 将 Generic `WorkflowProjectionFacade` 的最后一个 `reporting_adapter` 构造参数/属性更名为 `runtime_services`，并删除 AgentLoop 注释中已不存在的 `ReportingAgentRunner` 主调叙述；运行时仍把同一个 account-scoped services view 注入 `RuntimeBindingCatalog`，不改变路由、状态或 Capability 选择。Characterization First 先在 Application AST 上取得 `1 failed`，GREEN 后 Architecture/Projection/Binding/Nested HTTP focused `32 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未新增兼容 alias、Gate、Hash、CAS、锁、校验链或依赖。
- FA-07/M9.18 重跑最终架构关键路径 focused 选择：Architecture boundaries、Workflow Host、Typed Agent Turn、Agent Execution、Application Binding/Projection、五个 Reporting 公开入口、Module/Public/Template/Aggregate/Delivery 共 `113 passed, 1 deselected`。唯一缺口是 production Binding persisted completed-result RED：当前 `ModuleProviderRuntime` 能消费完整 `TaskCorrelation`，但 account Binding 没有当前语义 correlation；普通 `Work/runs/<run>/results/<task>.json` 只含 run/task/agent/session，Module 初稿 task id 在同一 Run 内稳定，无法区分用户补充、输入合同或目标变化后的旧结果。用旧 `TaskAttemptStore.current()` 作为 expected 会让旧 identity 自证，直接文件复用则可能错误跳过 Provider，因此两种简化方案都被拒绝。该 Characterization 已固化为 `strict xfail`，Binding focused 为 `6 passed, 1 xfailed`；安全等价路径只能机械复用已有 `TaskCorrelation` 的 envelope/profile/input/subject identity、`TaskAttemptStore` terminal/result integrity 和 `IdentityLeaseManager`。这会启用用户默认禁止的既有 Hash/CAS/lease 机制，故在解释并获明确许可前不实施。没有运行全量回归或真实 Provider。
- FA-07/M9.19 收到用户明确批准后，新增 Capability 内部 `ProviderTaskAttempt`，只编排既有 `IdentityLeaseManager → build_task_correlation → load_completed_agent_result/TaskAttemptStore.activate → release` 顺序；未复制或扩展其中的 Hash/CAS、terminal、lease 或身份算法。生产 `ModuleProviderRuntime` 与 `TemplateDistillationProviderRuntime` 现在都在 Provider session 创建前验证并复用 matching completed `AgentResult`，命中时 Provider/session 为 `0`，未命中才激活当前 physical attempt，完整 correlation 同时传给原 Submit/Blocked Tools。Module Binding strict xfail 已转为普通 GREEN；Module/Binding affected `33 passed`，Template focused `16 passed`。Cross/Chief/Final/FinalChief/Aggregate 的同等接线正在并行收敛；未跑全量，未进行真实 Provider/项目/浏览器测试。
- FA-07/M9.20 将同一 Capability 内部 attempt 生命周期接到 Cross Owner 与 Chief Chapter 的生产 Provider 组合和 typed bridges；两类已验证 completed `AgentResult` 都在 Provider/session 创建前由既有 Recovery policy 复用，未命中才激活当前 attempt，且 Submit/Blocked Tools 接收同一 correlation。既有“相同语义任务连续调用两次”的 Characterization 现在明确证明第二次不重放 Provider。Cross/Chief focused `10 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未新增或更改 Hash/CAS、lease、锁、Gate、身份/校验算法或依赖，未跑全量。
- FA-07/M9.21 将同一 production attempt/result 生命周期接到 Final Chapter、Final Chief Revision 与 Aggregate Editor Provider composition 和 typed bridges。所有七类 Provider 角色（Module、Template、Cross、Chief、Final、FinalChief、Aggregate）现在都会在 session/loop 创建前验证 matching completed `AgentResult`，未命中才激活当前 attempt，并将同一 correlation 交给原 Tool persistence。Final/FinalChief/Aggregate focused `12 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；该切片仍只编排用户批准的既有机制，没有新增或改变 Hash/CAS、lease、锁、Gate、身份/校验算法、依赖或公共接口，未跑全量。
- FA-07/M9.22 重跑最终自动架构与受影响行为选择：Architecture boundaries、Workflow Host、Typed Agent Turn、Agent Execution、Application Binding/Projection、五个公开 Reporting 入口、Module/Public/Template/Aggregate/Delivery 和全部生产 Provider recovery 共 `135 passed`；通用 React Run Workspace focused `10 passed`，定向 ESLint 与 TypeScript `--noEmit` 通过。生产扫描继续无旧 Runner/Facade/selector/Core Reporting 路径，测试树无 strict xfail。按用户要求未跑全量回归。自动阶段没有已知未完成项，下一步只执行一次真实 Provider/真实项目/浏览器验收。
- FA-07/M9.23 启动最终浏览器验收后，通用 `parameter-adjustment` 真实页面以输入 `4` 完成，输出 `10`、成本 `0 tokens`，证明当前构建的 Definition/Compiler/Generic Host/State/Events/React 投影连通。随后创建的 `full-report-1ab4a64b16ec480483cb4472b65c68ae` 在 Provider 调用前失败：验收服务器误以 `/Users/zzymima0000/Documents/Codex/test` 为 data root，使空的 `e84ec770-0736-4d85-8b7b-247accddfa26` 子目录成为项目；该 Run 的冻结 `input-snapshot.json` 与 manifest 均为 `files: []`，故 Preparation 在 `prepare-report-taxonomy` 正确拒绝缺失 S4-6 workbook。真实资料实际位于 `/Users/zzymima0000/Documents/Codex/test/Inputs`，正确服务器 data root 应为 `/Users/zzymima0000/Documents/Codex`、项目路由为 `/projects/test`。失败 Run 和空快照已保留，未篡改、未重试、Provider 成本为 `0 tokens`；这不是 Definition/Compiler/Provider 接线缺陷，但也不能计为 full-report 通过。最终剩余项是在正确 data root 上重新执行一次真实 Provider/项目/浏览器验收。
- FA-07/M9.24 已纠正本地服务器 data root 为 `/Users/zzymima0000/Documents/Codex`，通过可见 React 页面进入真实 `/projects/test/workflows`，启动有效 `full-report-ce7b78990cd0463b9c31996d13e5183b`。页面与服务端证据显示 Run 为 `running`，输入快照/Preparation/Readiness 已通过，文件定义与 Generic Host 已进入五个 Module lane 的 `prepare-current-module-authoring → invoke-current-module-author`，五个 `mimo-v2.5-pro` Provider stream 均已打开；启动时页面已有 `197` 个 workflow/action/tool/conversation 事件。按用户要求不持续高频监控，不创建第二个有效 Run；最终完成状态、WAITING 处理、Cross/Chief/Final/Delivery 产物与成本仍等待此同一 Run 的真实结果。
- FA-07/M9.25 同一真实 Run 推进到五个 Module Author 与四个 initial Reviewer 后暴露生产相关性缺陷：2.1/2.2/2.3/2.5 Reviewer 的 `AgentResult` 已以真实 `attempt-...` identity 成功持久化，但 `ModuleReviewerAgentBridge` 的 terminal predicate 仍使用测试桩遗留的空 `task_attempt_id`，导致 Generic Agent Turn 未接收完成消息、同 Session 进入 submission correction，并被既有不可变结果语义拒绝不同字节的重复提交。服务器已停止以避免继续消耗，Run、成功结果、原始提交和事件现场均保留，没有删除或重写。Characterization First 新增真实非空 attempt terminal 与 pre-session completed-result reuse 两项，旧实现 `2 failed`；修复后 Reviewer Bridge 原样接收现有 `TaskCorrelation.task_attempt_id`，并与 Author Bridge 一样在创建 Provider session 前调用已有 completed-result recovery，Module Provider 不再预先激活 Reviewer attempt。Reviewer/Module affected `31 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；没有新增或修改 Hash/CAS、锁/lease、Gate、身份算法、依赖或公共接口。下一步提交该切片并恢复同一 Run，验证四个已完成 Reviewer 不创建 Provider session，2.4 从已持久化 Author 结果继续，随后完成 Cross/Chief/Final/Delivery。
- FA-07/M9.26 真实验收进一步暴露并补齐通用进程重启恢复面：持久化状态为 `running` 并不表示当前进程仍拥有 detached task。新增业务中立 `POST /api/v1/runs/{run_id}/resume`、Capability binding/runtime resume 端口与按 Run 的 detached ownership；各 runtime 直接加载既有 Resolved Plan/WorkflowState 后继续 Generic Host，不重编译 Reporting 请求。Distribution Reporting 投影的 `active` 现在表示当前进程实际拥有该 Run；React 打开 `running + active=false` 的已有 Run 时显示“恢复运行”，成功后复用同一 Run ID 并继续轮询；WAITING 仍使用原 Schema 输入表单，FAILED 不被隐式重试，COMPLETED 幂等返回。后端 affected `29 passed`，前端 focused `12 passed`，定向 Ruff/ESLint/TypeScript、compileall、frontend build 与 `git diff --check` 通过。没有新增 Hash/CAS、锁/lease、Kernel Action、Capability ID 特判、依赖或业务门禁；下一步在可见页面恢复 `full-report-ce7b78990cd0463b9c31996d13e5183b` 并继续最终 Provider 验收。
- FA-07/M9.27 同一真实 Run 首次进程恢复暴露 Generic Host 只保存根状态、未把活动 Subworkflow/Parallel 分支的中间状态回写根 checkpoint：恢复后五个已完成 Module Author 被重新调度。服务器已立即停止，五个新 Provider stream 未被当作成功结果，原 Run、r0 Author/Reviewer 结果和事件现场全部保留。Characterization First 分别取得嵌套第二步运行时缺少 child checkpoint、并行已完成兄弟缺少 branch result 的 RED；修复后 Host 将每次嵌套 Kernel transition 和并行分支状态/结果递归投影到唯一根 `WorkflowState` 并调用既有 state store，Kernel、Action 与业务定义均未改变。针对该已丢失旧 checkpoint 的真实 Run，Capability 在显式 same-run resume 时从既有 promoted `results/module-<id>.json` 恢复 typed `ModuleSubmission`，使 Author 不重放，并以原 r0 输入重建 Reviewer correlation；不扫描历史 attempt、不放宽 identity、不改写结果。Workflow Host focused `18 passed`，Module/Public affected `7 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未跑全量，未新增 Hash/CAS、锁/lease、Gate、校验算法、依赖或公共接口。下一步提交并从页面恢复同一 `full-report-ce7b78990cd0463b9c31996d13e5183b`，预期四个已完成 Reviewer 零 Provider 复用，仅缺失的 2.4 Reviewer 发起真实调用，然后继续 Cross/Chief/Final/Delivery。
- FA-07/M9.28 继续恢复同一真实 Run 后确认 M9.27 的根与嵌套 checkpoint 已生效：五个初稿均从同 Run 结果恢复，2.1/2.2/2.3/2.5 的后续 Provider 调用实际是 Reviewer finding 要求的合法 revision，2.4 则先完成缺失 initial review；其 Max Token continuation 与同 Session 结构化纠正均由现有 Recovery 路径成功完成。随后真实 revision 暴露 Capability Provider composition 的阶段投影错误：累计 lane context 同时保留 author/review/revision 时，`ModuleProviderRuntime` 按字段先后取到旧 Reviewer envelope，使 Specialist 的 Submit Tool、TaskAttempt 与 terminal correlation 仍指向 `module-*-initial-review-r0`，现场表现为 revision 提交错误类型及既有 Reviewer attempt 的 immutable-bytes 冲突。服务器已停止，Run 与错误 attempt/result/event 均保留。Characterization First 用同一累计 context 的四种文件定义 Task 先取得 `TypeError` RED；实现后单一 Capability 投影按当前 `TaskDefinition.output_contract` 精确选择 author/review/revision/recheck envelope，Provider attempt、Tool composition 与 Author bridge 共用该投影，Kernel/Compiler/Host 未改。Module Provider/bridge/completed-result 与 Public finding→revision→recheck affected `7 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未跑全量，未新增或更改 Hash/CAS、锁/lease、Gate、校验算法、依赖或公共接口。下一步提交后从页面继续恢复同一 Run，验证 revision 使用 `module-revision-rN-*` correlation 并推进 Cross/Chief/Final/Delivery。
- FA-07/M9.29 同一真实 Run 验证 M9.28 后，五个 Specialist 均已使用正确的 `module-revision-r1-2.x` task/agent/input correlation，证明阶段 envelope 投影修复进入真实生产路径；随后五路一致以 `module part is not ready for commit` 确定性失败。原始 correction 明确报告当前工具环境不存在 `write_result_part`。现场追溯到文件任务 `module-*-runtime-revision.yaml` 已声明完整工具集，但持久化业务 envelope 仅含 `submit_result`，Artifact Access 编译时错误以 envelope 覆盖当前 `TaskDefinition.tools`。Characterization First 用同形 envelope 先证明正文工具被裁掉，GREEN 后 Module Provider 以当前文件任务工具作为执行作用域，envelope 继续只负责任务身份、输入与 artifact 范围；既有已持久化 Run 无需改写即可在恢复时获得 `write_result_part/list_result_parts/submit_result`。真实日志同时捕获流式 `max_tokens` 半截工具调用访问不存在 `StreamResponse.usage`；独立 RED 固化后以 `getattr` 机械读取现有 usage，不新增重试或判断策略。Module/Public affected `5 passed`，Agent Loop Max Token/Submit correction `3 passed`，定向 Ruff（Agent Loop 保留两个既有 N818 命名告警时忽略该规则）、compileall 与 `git diff --check` 通过。未跑全量，未修改 Kernel/Compiler/Host，没有新增 Hash/CAS、锁/lease、Gate、校验算法、依赖或公共接口。下一步提交并恢复同一 Run，验证 Revision 实际写入 scoped result parts 后继续 Recheck、Cross、Chief、Final 与 Delivery。
- FA-07/M9.30 在准备恢复 M9.29 的同一真实 Run 时，网页只对 inactive `running` 暴露“恢复运行”，四个 Reporting 文件 Runtime 与 Parameter Adjustment 又显式拒绝 `FAILED`，导致修复后的失败 action 无法从通用产品界面继续。Generic Host 已有 Characterization 证明其从持久化 `FAILED` 状态只重试失败 Subworkflow/Parallel branch 并保留已完成兄弟，因此缺口不在 Kernel。新增后端/前端 Characterization 先分别得到 `CapabilityRunStateError: failed runs require an explicit retry` 和缺少恢复按钮的 RED；GREEN 后既有 `POST /runs/{run_id}/resume` 统一接收 inactive `running/failed`，`WAITING` 仍必须走 Schema 输入接口，`COMPLETED` 仍幂等。没有新增 retry API、Action Kind、状态算法或 Capability 特判。后端 Runtime/Host/Parameter affected `15 passed`，React Run Workspace `12 passed`，定向 Ruff/ESLint/TypeScript、compileall 与 `git diff --check` 通过。未跑全量，未新增 Gate、Hash、CAS、锁、校验链、依赖或公共接口；下一步重启当前服务构建并从网页恢复同一 `full-report-ce7b78990cd0463b9c31996d13e5183b`。
- FA-07/M9.31 同一真实 Run 在 M9.30 恢复后让五个 Revision Provider 生成了 typed 结果，但 `ProviderTaskAttempt.acquire` 对已有非 completed terminal 仍沿用原 `task_attempt_id`，新结果因既有 append-only attempt 路径不可变而一致失败；服务器已停止，旧 failed terminal、结果、Conversation/Session 与 Run 现场均保留。Characterization First 先证明同一失败 terminal 的恢复仍得到 `attempt-failed` RED；GREEN 后只在当前 physical attempt 已有可验证且非 completed terminal 时生成新的 `attempt-*`，同一 run/task/agent/session 和 semantic correlation 保持不变，旧 failed 与新 completed attempt 可分别读取。既有 completed terminal 继续在 Provider/session 创建前复用，不发生重放。失败 attempt/七类 Provider completed reuse/Generic Host FAILED 恢复 focused `10 passed, 16 deselected`，定向 Ruff、compileall 与 `git diff --check` 通过；未跑全量，未修改 Kernel/Compiler/Host，也未新增或改变 Hash/CAS、锁/lease、Gate、身份/校验算法、恢复 API、依赖或公共接口。下一步提交并重启单一服务实例，从网页恢复同一 `full-report-ce7b78990cd0463b9c31996d13e5183b`，继续 Recheck、Cross、Chief、Final 与 Delivery。
- FA-07/M9.32 用 M9.31 构建从可见页面恢复同一真实 Run 后，未进入 Provider 即捕获 `persisted task attempt identity or hash mismatch`：此前旧进程曾在同一 failed `task_attempt_id` 上取得新 lease 并更新 `current.json`，而 append-only terminal 仍正确保存原 lease correlation。Characterization First 精确重现“terminal lease epoch 1、current pointer lease epoch 2”的现场并取得 RED；GREEN 后 `TaskAttemptStore` 将原有 result/payload hash 验证机械抽为按 immutable task/attempt 路径读取，严格 `load_verified_result(later_pointer)` 仍按原行为拒绝 correlation 不相等，Provider 恢复则在验证 terminal 自身及既有 semantic identity 后轮换 physical attempt。没有吞掉损坏、放宽 hash 或改写旧 terminal。两种 failed-attempt 形态与七类 Provider completed reuse focused `9 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未跑全量，未修改 Kernel/Compiler/Host，没有新增 Hash/CAS、锁/lease、Gate、校验算法、恢复 API、依赖或公共接口。下一步提交、重启单一服务并从网页再次恢复同一 Run。
- FA-07/M9.33 同一真实 Run 的五个 Revision Provider 已使用正确 task/session 和完整声明 Tool scope 写出目标小节正文，但均在 `module part is not ready for commit` 处确定性失败；现场证明所有正文存在而对应 `_evidence/<part>.json` 缺失。根因是 Capability `build_module_provider_tools` 只对 initial `module_submission` 启用既有 evidence-bound result-part contract，revision 的 `module_revision_submission` 被错误当成普通 result part，导致 `write_result_part(..., evidence_ids=...)` 拒绝持久化，而同一个既有 `SubmitResultTool` 又要求 revision prose 与显式 evidence binding 同时存在。Characterization First 使用真实 Capability Tool composition 先取得 `persisted=False` RED；GREEN 后仅让 `module_revision_submission` 复用原 `WriteResultPartTool/ListResultPartsTool/SubmitResultTool` 的 evidence binding 语义，没有新增 validator、判断门禁、Hash/CAS、锁/lease、恢复策略、依赖或公共接口。新 focused 与受影响 Module Provider/Revision 选择 `2 passed, 30 deselected`，定向 Ruff、compileall 与 `git diff --check` 通过；未跑全量。下一步提交并恢复同一 Run，验证五个 revision attempt 能写出 evidence binding，随后继续 Recheck、Cross、Chief、Final 与 Delivery。
- FA-07/M9.34 M9.33 构建恢复同一真实 Run 后，五个 Revision Provider 均生成了 15 份 evidence binding，证明 revision result-part 修复进入真实生产路径；但旧 checkpoint 携带的 failed physical attempt A 在 `current.json` 已指向另一个 failed attempt B 时，会跳过 A 自身已有的不可变 terminal/result，重新激活 A，最终以 `immutable task attempt result already exists with different bytes` 确定性失败。服务器已立即停止，Run、旧失败结果、Conversation/Session、15 份 evidence binding 与事件现场全部保留。Characterization First 新增“A failed → B failed/current → 从旧 checkpoint A 恢复”的测试并取得旧实现重新使用 A 的 RED；GREEN 后 `ProviderTaskAttempt.acquire` 直接用既有 `TaskAttemptStore.load_verified_attempt(task_id, task_attempt_id)` 验证 checkpoint 指向的物理 attempt，非 completed terminal 轮换新 physical attempt，semantic run/task/agent/session 不变。没有新增 Hash/CAS/锁/Gate/判断算法，也没有放宽或复制既有 result/hash 完整性验证。failed-attempt 三种形态与七类生产 Provider completed-result reuse focused `10 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未跑全量。按用户要求，Main 单一对话、三种 Lane 恢复交互、侧边栏与通用界面产品化继续列为待做，当前优先恢复同一 Run 证明 Recheck → Cross → Chief → Final → Delivery 能完整跑通。
- FA-08/M9.35 追溯 Plan base 与全 Git 历史确认 Capability `gates/` 从未存在生产 YAML，旧 “gate” 业务语义已分别由 Contract、Parallel/Join、Workflow `if`、Interaction 和 Recovery 承接；当前 `GateDefinition/EvaluateGate` 仅为未使用的早期框架预留。新的架构 Characterization 先捕获 8 个 Capability 文件反向导入 `manyselves.application.runtime_services` 的 RED；GREEN 后只读 `RuntimeServicesView` 物理下沉到 `manyselves.runtime.services`，Application 仅负责从 Account RuntimeHost 构造它，Capability 不再导入 Application。Application/Architecture/Distribution Binding focused `25 passed`，定向 Ruff 与 `git diff --check` 通过；未跑全量，未新增 Gate、Hash/CAS、锁、判断算法、依赖或公共接口。最终真实 Provider 验收按用户要求暂缓，下一切片继续 core/runtime 物理所有权与旧测试执行器收敛。
- FA-08/M9.36 为未使用 Gate 表面新增架构 Characterization，先因 `GateDefinition/EVALUATE_GATE` 仍在 Kernel 得到 RED；GREEN 后物理删除 DefinitionKind/Capability location/Workflow field、Gate model、EvaluateGate Action/Executor、Compiler reference/snapshot/CFG 分支与全部空 `gates/.gitkeep`，并机械移除 40 个生产 Workflow 的空 `gates: []`。原 Gate 测试没有保留兼容接口：结构校验继续由 `validate_contract`，条件路由继续由 `if/fail_workflow` Characterization 覆盖；Parallel/Join、Interaction、Recovery 生产定义均未改变。Definitions/Compiler/Control Flow/Application/Architecture/Web projection/Distribution Binding focused `100 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未跑全量，未新增 Gate、Hash/CAS、锁、判断算法、依赖或公共接口。下一切片移除生产包中的测试专用 Sequential/ControlFlow 执行器。
- FA-08/M9.37 为“发布包只能存在一个 Workflow 执行宿主”新增架构 Characterization，先因测试专用 `SequentialWorkflowExecutor` 与 `ControlFlowWorkflowExecutor` 仍位于生产包而 RED；GREEN 后将其行为测试统一迁到现有 `WorkflowRuntimeHost`（测试只注入内存 Event Sink），物理删除两个旧执行器及导出，不保留 shim 或第二套运行时。旧执行器曾把所有 Action 计入 `control_steps`，唯一 Host 已有语义只计 `If/ConditionGroup/Goto/ForEach` 控制推进，本次将内部断言对齐为 5，循环结果仍为 3；没有改变 Kernel transition 或运行上限逻辑。Workflow control/sequential/interaction/tool-adapter 与架构 focused `35 passed`，定向 Ruff、compileall、旧符号扫描与 `git diff --check` 通过；未跑全量，未新增 Gate、Hash/CAS、锁、判断算法、依赖或公共接口。下一切片继续 core/runtime 物理所有权收敛。
- FA-08/M9.38 为“Runtime 与 Capability 生产代码不得导入旧 Core 执行 substrate”新增架构 Characterization，先得到 25 个违规文件的 RED；GREEN 后将通用 Agent Loop/MessageBus、Provider、Tool、Artifact、Prompt、Checkpoint、Usage、runtime errors 与 access policy 作为完整无环组件物理迁入 `manyselves/runtime`，全仓生产/测试 import 与 monkeypatch target 机械更新，旧 Core 不留 shim 或 convenience re-export。`manyselves/core` 现在只保留 Conversation、项目结构、设置/历史等项目和产品服务；Capability 与 Runtime 对 Core 导入扫描为零。对应 `tests/core/{artifacts,loops,tools}` 也迁到 `tests/runtime`，当前项目指南、团队定义与发布矩阵路径同步更新。架构/发布矩阵 `18 passed`，Runtime substrate focused `196 passed`，Distribution Provider/ownership affected `37 passed`；定向 Ruff（只保留 AgentLoop 两个既有 N818 命名例外）、compileall、import scan 与 `git diff --check` 通过，未跑全量。额外选择发现 Template 文件入口的两个 typed 输入测试在本切片前的干净 `ed76ec2` 也同样失败，已用隔离 worktree 证明不是 import 迁移引入；下一切片按 Characterization 修复，不混入本物理所有权提交。没有新增 Gate、Hash/CAS、锁、判断算法、依赖或公共接口。
- FA-08/M9.39 修复 Template 文件入口的 typed 根输入恢复偏差：Characterization 已在干净 `ed76ec2` 证明两个 Host 入口因把 `PublicTemplateDistillationRequest` 误按领域 `TemplateDistillationInput` 解码而失败；GREEN 后 Capability Runtime 在文件 Workflow 根变量处按公共 Schema 模型读取 `template_ref`，Host-owned `run_id` 直接来自现有 `WorkflowState.run_id`，已完成领域输入恢复仍保持原分支。Kernel、Compiler、Host、Workflow YAML 与公共接口均未改。Template 入口/Provider/Recovery focused 全文件 `16 passed`，定向 Ruff、compileall 与 `git diff --check` 通过；未跑全量，未新增 Gate、Hash/CAS、锁、判断算法或依赖。
- FA-08/M9.40 将通用 Run 交互投影到 Main 单一对话：新增只读 `GET /api/v1/runs`，由 Application 从当前 account workspace 的通用 `WorkflowStateStore` 列出根 Run 并复用既有 `get_run` 投影；Main React 只按 `waitingInput + JSON Schema` 渲染 Interaction，枚举标签由 Capability Pydantic Schema 的 `x-enum-labels` 提供，Lane 显示只读取通用 `path.branch_id`，提交继续使用既有 `/runs/{run_id}/input` 恢复同一 Run。inactive `running/failed` 也在 Main 暴露既有 `/resume`，不新建 Run。Characterization First 分别取得缺少 Run feed、缺少三选一 Schema 标签和缺少 Main WAITING 卡片的 RED；GREEN 后后端投影/Schema focused `19 passed`，Conversation/RunWorkspace/AppLayout `22 passed`，架构/发布矩阵 `20 passed`，定向 Ruff/ESLint/TypeScript、OpenAPI 生成一致性与 `git diff --check` 通过。会话路由 Characterization 同时证明 `AppLayout` 始终保留 Sidebar；前端没有 Capability ID、Workflow ID、Reporting 或模块编号分支。未跑全量，未新增 Gate、Hash/CAS、锁、判断算法、重试、依赖或 Capability 专属 HTTP 接口。
- FA-08/M9.41 清除通用发布包中残留的 Distribution Reporting 领域语义：Runtime 默认 Prompt 改为只描述当前 Task definition 与可用 Tools；项目脚手架只创建 `Inputs/Knowledge/Templates/Work/runs/Outputs`，Reporting 的 `Modules/Reviews/Reports` 目录继续由 Capability-owned store 按需创建；通用 Agent label 不再内置 Editor/Auditor/Cross/Chief/Template Distiller 角色映射；专家模板访问政策物理迁到 Capability，并通过 `InspectDocumentTool.path_validator` 这一中立注入点保留原隔离行为，通用 File/Exec/Artifact 工具不再识别领域文件名。Characterization First 分别以 Prompt 业务词、项目领域目录、通用角色 ID 和模板访问规则取得 RED；GREEN 后通用/架构选择 `85 passed`、Capability/Artifact 受影响选择 `59 passed`，定向 Ruff、compileall、业务标识扫描与 `git diff --check` 通过。一个 Module recovery 测试的 Provider envelope 缺少其声明 Tool 所需 artifact ref，已在测试夹具补入真实输入引用；隔离基线证明该失败早于本切片。未跑全量，未新增 Gate、Hash/CAS、锁、判断算法、重试、依赖或公共接口。
- FA-08/M9.42 移除通用 React 输出页对 `Outputs/Modules`、`Outputs/Reports`、`Outputs/Reviews` 的硬编码：OutputTabs 现在只从文件 API 返回的 `Outputs` 第一层目录或根文件动态生成分类，分页、预览、下载和删除继续消费同一通用 `FileEntry`；因此新 Capability 可以使用任意产物目录而无需修改前端。Characterization First 以仅含 `Outputs/Deliverables/result.json` 的项目树取得缺少 Deliverables 且错误显示三个 Reporting tab 的 RED；GREEN 后 ProjectDirectoryPage focused `14 passed`，定向 ESLint、TypeScript 与 `git diff --check` 通过。未跑全量，未新增 Capability/Workflow ID 分支、Gate、Hash/CAS、锁、判断算法、依赖或公共接口。
- FA-08/M9.43 区分并清理了与 Workflow Gate 无关的 AgentLoop 死代码：Git 全历史复核再次证明 Capability `gates/` 只有 `.gitkeep`、所有生产 Workflow 的 `gates` 都是空列表；同时发现旧 `ContextGateDecision/pre_send_context_gate` 是未被任何生产 composition 注入或调用的 Provider 前重复内容哈希判定，并非 Definition/Compiler/Kernel 控制流。Characterization First 分别以旧符号/指纹函数存在、未使用 `pre_send_context_guard` 注入面存在和通用 Agent 执行源码仍使用 Reporting 所有权词取得 RED；GREEN 后删除整套未使用的 payload/result/evidence 指纹、默认阻断算法、可选 guard 参数和历史 request 缓存，只保留真正使用的 typed context-budget exhaustion、Provider retry、Recovery、Tool-result/Conversation reuse；私有 trim 类型和注释统一为 Capability-neutral 命名。AgentLoop 全受影响文件 `78 passed`，Runtime execution/ledger focused `33 passed`，架构/发布矩阵 `23 passed`，定向 Ruff、compileall 与 `git diff --check` 通过。未跑全量；本片是删除未使用 Gate/Hash 逻辑，没有新增 Gate、Hash/CAS、锁、判断算法、重试、依赖或公共接口。
- FA-08/M9.44 完成最终自动发布图审计：五个 Distribution Reporting 公开根均从文件定义编译成功，分别形成 `full-report 14 actions/38 subworkflows/310 snapshots`、`module-report 13/8/169`、`aggregate-existing 3/16/90`、`render-existing 3/0/8`、`distill-template-skill 7/0/24`；Parameter Adjustment 同一 Compiler/Host focused `9 passed`。Definition/Compiler/Kernel/Host/Application/Web focused `107 passed`，Reporting 五根定义与拓扑 `13 passed`，架构/发布矩阵 `23 passed`，最终 Main/Run/File/Sidebar React 选择 `36 passed`，定向 Ruff/ESLint、compileall、TypeScript 与 `git diff --check` 通过。生产扫描确认 Kernel Action 仅有通用变量、Tool、Agent、Conversation、控制流、Contract、Interaction、Output 与终态动作；Generic Python/React 无 Distribution entrypoint、Reporting 输出目录或旧 Runner/Facade/API 引用；Git 发布树无 Gate 文件。Compiler/Definition/semantic trace 及两个 Capability bridge 的过期 WP/Legacy 所有权说明已同步为当前形态。未跑全量，也未执行真实 Provider/项目/浏览器；后者是当前唯一剩余验收项。
- 本阶段没有运行全量回归；真实 Provider Run 的失败现场与同一 Run 恢复链均保留且按用户要求暂不恢复。Main 对话 Run 投影、三种 Lane 选择、原 Run 中断恢复、侧边栏及通用 Output 产品化与最终自动架构审计均已完成。

## Research decisions

- Microsoft Agent Framework: `reference`；官方当前 Declarative Workflow 证明 YAML→Executable Graph、Function Tool、HITL、Checkpoint/Subworkflow 可行。
- LangGraph: `reference`；官方当前 Graph API 证明 State/Node/Edge、compile、parallel/subgraph/persistence 可行，并明确 Workflow 与 Agent 的职责差异。
- Production runtime: 保持 Manyselves 内部轻量 Loader/Compiler/Kernel/Host；不新增外部编排依赖。
- Capability architecture: 每个 Capability 有 Domain Runtime/Tools，不复制 Kernel。
- Legacy compatibility: 已废止为最终要求；不新增 Adapter/Feature Flag 延长双路径。

完整来源和 POC 解释见 [`../research/DECLARATIVE_RUNTIME_LANDSCAPE.md`](../research/DECLARATIVE_RUNTIME_LANDSCAPE.md)。

## Active user constraints

- 默认只运行 focused tests 和受影响测试，不做全量回归，除非用户明确要求；
- Characterization First；
- Kernel 业务无关；
- 不把 Reporting 角色或流程变成 Kernel Action；
- 不新增生产编排框架；
- 默认禁止新增不必要的 Gate、判断门禁、Hash、CAS、锁和额外校验链；确有需要必须先解释并获得批准；
- 保留结构化纠正、Schema 原 Conversation 修正、Max Token、Tool Slice、No-progress、Tool Result 复用、Conversation/Session 复用和 Same-run 恢复；
- 中途不设置人工验收断点，自动推进到最终架构完成；
- 最后只进行一次真实 Provider/项目/浏览器测试；真实长任务不持续高频监控，失败保留同一 Run 和现场。

## Verification policy

每个切片记录实际命令。默认：

```bash
uv run ruff check <changed-python-and-test-paths>
uv run pytest -q <focused-and-affected-tests> --maxfail=3
git diff --check
```

前端只运行 focused Vitest、定向 ESLint、TypeScript 或受影响 build。任何窄选择都不得被描述为全仓库回归。

## Completion evidence matrix

最终完成前必须逐项填入当前证据：

| Requirement | Evidence status |
| --- | --- |
| Stateless Kernel business-neutral | `achieved by generic Action list, definition/import scans and focused architecture Characterization` |
| File definitions → complete ResolvedPlan | `achieved; definitions/registry/compiler/control-flow/interaction/host/application projection focused audit 107 passed` |
| One Generic Runtime Host | `achieved for all five public Reporting entrypoints; alternate packaged Sequential/ControlFlow executors removed` |
| Generic Agent/Tool/Conversation/Recovery | `Agent Loop/Provider/Tool/Artifact/Recovery are physically Runtime-owned; Capability and Runtime no longer import Core execution substrate` |
| Capability-owned Reporting Domain Runtime | `achieved; final real Provider execution remains under acceptance` |
| No Declarative→Legacy Runner inheritance/delegation | `achieved; old Runner/Facade/Core package deleted` |
| Generic Capability Binding/Application | `achieved; RuntimeServicesView is Runtime-owned and Capability no longer imports Application` |
| Generic FastAPI/React for two capabilities | `achieved; Schema/WAITING/Event/Output/Cost generic projection` |
| Legacy Runner/Facade/selectors absent from production/release | `achieved by production scan and boundary tests` |
| Recovery/Same-run behaviors on final path | `correction/continuation/no-progress/session/tool-result/persisted Agent completion and failed physical-attempt rotation achieved across all production Provider roles; final real validation pending` |
| Focused/affected checks and builds | `final Definition/Compiler/Host 107 passed; public roots 13 passed; AgentLoop 78 passed; Runtime/architecture 56 passed; frontend 36 passed; targeted Ruff/ESLint/TypeScript passed` |
| Final real Provider/project/browser test | `parameter-adjustment passed; Reviewer terminal/process-restart/nested-checkpoint/revision-envelope/failed-attempt defects fixed; same full-report Run final outputs pending` |
| Docs/code/tests/release consistent | `automatic architecture evidence aligned; only final real-test result deferred` |

只要一项仍为 partial、pending、missing 或 indirect，就不得宣称项目完成。

## Next automatic sequence

1. `completed` — `manyselves/core` 与 `manyselves/runtime` 的通用运行时物理所有权已收敛；
2. `completed` — Main 单一对话与三种 Lane 恢复选择已接入通用 Run/Interaction 投影；
3. `completed` — 侧边栏、Run Output/Event/Cost、通用 Schema/WAITING 和动态 Output 分类已完成产品化复审；
4. `completed` — 最终自动架构/发布图审计已完成；
5. `deferred by user` — 最终真实 Provider/项目/浏览器测试，恢复时继续使用已保留现场或新建明确验收 Run。

## Resume instruction

新会话执行：

```text
Run git status -sb, git branch --show-current, git log -1 --oneline.
Read AGENTS.md and this status file completely.
Continue from Current FA work package / Next automatic sequence.
Do not restore Legacy compatibility as a target and do not ask between slices.
```
